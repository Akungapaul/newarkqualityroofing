---
phase: 01-foundation-and-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/app/not-found.tsx
autonomous: true

must_haves:
  truths:
    - "Running `next build` succeeds with zero errors"
    - "Running `next dev` shows a page at localhost:3000 with Cormorant heading and Cormorant Garamond body text"
    - "The forest green, copper, and parchment colors are available as Tailwind utilities (bg-forest, text-copper, bg-parchment)"
    - "No tailwind.config.js exists -- all tokens defined in CSS @theme"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 import + @theme design tokens"
      contains: "@import \"tailwindcss\""
    - path: "src/app/layout.tsx"
      provides: "Root layout with Cormorant + Cormorant Garamond fonts as CSS variables"
      contains: "Cormorant"
    - path: "next.config.ts"
      provides: "Next.js 16 configuration"
    - path: "postcss.config.mjs"
      provides: "Tailwind v4 PostCSS plugin"
      contains: "@tailwindcss/postcss"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS variable injection from next/font"
      pattern: "--font-cormorant"
    - from: "src/app/globals.css"
      to: "Tailwind utilities"
      via: "@theme token definitions"
      pattern: "--color-forest"
---

<objective>
Set up the Next.js 16 project with TypeScript, Tailwind CSS v4, and the Editorial Trust design system tokens (Cormorant fonts + forest green/copper/parchment palette).

Purpose: Creates the project skeleton that all subsequent plans build on. Without a working Next.js project with design tokens, nothing else can proceed.
Output: A buildable Next.js 16 project with Tailwind v4 @theme design tokens and both Cormorant font families loaded.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-and-data-layer/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 16 project with TypeScript and Tailwind v4</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    postcss.config.mjs
    src/app/page.tsx
    src/app/layout.tsx
    src/app/globals.css
  </files>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` in the project root directory `/Users/akungapaul/Desktop/N8NProjects/Newarkqualityroofing`. Accept defaults. The project root already has a `design-previews` folder and `.planning` folder -- create-next-app should work in the existing directory.

    After scaffolding:
    1. Install Zod: `npm install zod@3` (use Zod 3.24.x for stability, NOT Zod 4)
    2. Verify `postcss.config.mjs` uses `@tailwindcss/postcss` (Tailwind v4 pattern). If create-next-app generated a different config, replace with:
       ```js
       const config = { plugins: { "@tailwindcss/postcss": {} } };
       export default config;
       ```
    3. Verify NO `tailwind.config.js` or `tailwind.config.ts` exists. If it was generated, DELETE it -- Tailwind v4 uses CSS @theme instead.
    4. Verify `next.config.ts` exists (Next.js 16 uses TypeScript config by default). Keep it minimal.
    5. Create `src/app/not-found.tsx` with a simple 404 page that uses the design system colors.

    IMPORTANT: This is Next.js 16 (latest stable). Key differences from 15:
    - params are async Promises (must await)
    - Turbopack is default bundler
    - No `next lint` command -- use ESLint directly
    - No tailwind.config.js -- use CSS @theme
  </action>
  <verify>
    Run `npm run build` -- must complete with zero errors.
    Run `npm run dev` and verify localhost:3000 loads.
    Confirm no `tailwind.config.js` or `tailwind.config.ts` exists.
    Confirm `zod` is in package.json dependencies.
  </verify>
  <done>
    Next.js 16 project builds successfully. Zod 3.24.x installed. Tailwind v4 configured via PostCSS with no JS config file. Project is ready for design tokens.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Editorial Trust design system tokens and typography</name>
  <files>
    src/app/globals.css
    src/app/layout.tsx
    src/app/page.tsx
    src/app/not-found.tsx
  </files>
  <action>
    **1. Configure globals.css with @theme design tokens:**

    Replace the contents of `src/app/globals.css` with Tailwind v4 @theme tokens. Structure:
    ```css
    @import "tailwindcss";

    @theme {
      /* Editorial Trust Color Palette */
      --color-forest: #1A3A2A;
      --color-forest-light: #2A5A3A;
      --color-forest-dark: #0F2218;
      --color-copper: #C17F4E;
      --color-copper-light: #D4A574;
      --color-copper-dark: #A66A3A;
      --color-parchment: #F5F0E8;
      --color-parchment-dark: #E8E0D0;
      --color-parchment-light: #FAF8F4;

      /* Semantic colors */
      --color-text-primary: #1A1A1A;
      --color-text-secondary: #4A4A4A;
      --color-text-on-dark: #F5F0E8;
      --color-text-on-copper: #FFFFFF;
      --color-border: #D4CFC4;

      /* Typography -- connected to next/font CSS variables */
      --font-heading: var(--font-cormorant), serif;
      --font-body: var(--font-cormorant-garamond), serif;
    }
    ```

    Add base layer styles after @theme for smooth defaults:
    - Body: `font-body bg-parchment text-text-primary`
    - Headings (h1-h6): `font-heading`
    - Selection color using copper/parchment
    - Smooth scrolling on html

    **2. Configure layout.tsx with Cormorant fonts:**

    Import both `Cormorant` and `Cormorant_Garamond` from `next/font/google`. Configure:
    - Cormorant: weights 300-700, variable `--font-cormorant`, display swap
    - Cormorant_Garamond: weights 300-700, variable `--font-cormorant-garamond`, display swap
    - Apply both CSS variables to the `<html>` element className
    - Set `lang="en"` on html
    - Set metadata: title "Newark Quality Roofing | Expert Roofing Services in Essex County, NJ", description about roofing services

    NOTE: If `Cormorant_Garamond` import name doesn't work, try `CormorantGaramond`. The research flagged this as needing runtime verification.

    **3. Update page.tsx as a temporary homepage:**

    Create a minimal homepage that demonstrates the design tokens are working:
    - An h1 using font-heading class with forest green color
    - A paragraph using font-body class
    - A copper-colored accent element
    - Parchment background visible
    - This is temporary -- will be replaced in Phase 2

    **4. Update not-found.tsx:**

    Simple 404 page using the design system tokens -- forest green heading, parchment background, copper accent on a "Go Home" link.
  </action>
  <verify>
    Run `npm run build` -- must complete with zero errors.
    Run `npm run dev` and verify:
    1. Heading text renders in Cormorant font (decorative serif)
    2. Body text renders in Cormorant Garamond font (readable serif)
    3. Forest green (#1A3A2A) is visible on the page
    4. Copper (#C17F4E) accent is visible
    5. Parchment (#F5F0E8) background is applied
    6. Tailwind utilities work: `bg-forest`, `text-copper`, `bg-parchment`, `font-heading`, `font-body`
  </verify>
  <done>
    Design system tokens are defined in CSS @theme. Both Cormorant font families load correctly via next/font. All color utilities generate from @theme. The temporary homepage visually demonstrates the Editorial Trust palette and typography.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npm run dev` shows the temporary homepage at localhost:3000
3. Browser DevTools confirm Cormorant and Cormorant Garamond fonts are loaded
4. Tailwind classes `bg-forest`, `text-copper`, `bg-parchment`, `font-heading`, `font-body` all generate correctly
5. No `tailwind.config.js` or `tailwind.config.ts` exists in the project
6. `zod` package is installed at version 3.x
</verification>

<success_criteria>
- FNDN-01: Next.js 16 project with TypeScript, Tailwind CSS v4 -- DONE
- FNDN-06: Cormorant (headings) + Cormorant Garamond (body) fonts via next/font -- DONE
- FNDN-07: Forest green / copper / parchment palette with CSS variables -- DONE
- The project builds and serves pages successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-layer/01-01-SUMMARY.md`
</output>
