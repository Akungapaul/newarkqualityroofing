---
phase: 09-conversion-and-design-polish
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/data/site-config.ts
  - src/components/sections/TrustBar.tsx
  - src/components/sections/TestimonialCarousel.tsx
  - src/components/sections/CompactTestimonial.tsx
  - src/components/sections/BeforeAfterGallery.tsx
  - src/app/page.tsx
  - src/components/templates/ServiceTemplate.tsx
  - src/components/templates/CityTemplate.tsx
autonomous: true
requirements: [DSGN-02, DSGN-03, DSGN-04]

must_haves:
  truths:
    - "Trust bar shows text-only stats with custom SVG icons in copper — no badge images (GAF, BBB, HomeAdvisor removed)"
    - "Trust bar numbers animate from 0 to target value on viewport entry"
    - "Testimonial carousel displays aggregate rating header ('4.9 out of 5 — based on 200+ reviews') above cards"
    - "No outbound review links (Google, HomeAdvisor, BBB links removed from testimonial section)"
    - "Before/after gallery on homepage has 4 slider items (2 residential + 2 commercial) with drag handles"
    - "Service and city pages show compact inline testimonials (1-2 relevant, no carousel)"
    - "Gallery images have CSS brand treatment (.photo-treatment class)"
    - "Gallery items include ImageObject JSON-LD schema"
  artifacts:
    - path: "src/components/sections/TrustBar.tsx"
      provides: "Text-only trust bar with SVG icons and CountUp animation"
      contains: "CountUp"
    - path: "src/components/sections/TestimonialCarousel.tsx"
      provides: "Enhanced carousel with aggregate rating header, no outbound links"
      contains: "4.9 out of 5"
    - path: "src/components/sections/CompactTestimonial.tsx"
      provides: "1-2 inline testimonials for service/city pages"
      exports: ["CompactTestimonial"]
    - path: "src/components/sections/BeforeAfterGallery.tsx"
      provides: "Slider comparison gallery with drag handle, 4 project items"
      exports: ["BeforeAfterGallery"]
  key_links:
    - from: "src/components/sections/TrustBar.tsx"
      to: "src/components/animations/CountUp.tsx"
      via: "import CountUp for stat animation"
      pattern: "import.*CountUp"
    - from: "src/components/sections/BeforeAfterGallery.tsx"
      to: "src/app/globals.css"
      via: ".photo-treatment CSS class on slider containers"
      pattern: "photo-treatment"
    - from: "src/app/page.tsx"
      to: "src/components/sections/BeforeAfterGallery.tsx"
      via: "Inserted between ResidentialCommercial and TestimonialCarousel"
      pattern: "BeforeAfterGallery"
---

<objective>
Redesign the trust bar to text-only with SVG icons and count-up animations, enhance the testimonial carousel with aggregate rating and remove outbound links, create compact testimonial variant for inner pages, and build the before/after gallery slider component for the homepage.

Purpose: These are the core trust signal components that drive conversion. The trust bar replaces badge images with editorial-quality text cards, the gallery provides social proof via project showcase, and compact testimonials bring endorsements to service/city pages without a heavy carousel.

Output: Redesigned TrustBar, enhanced TestimonialCarousel, new CompactTestimonial, new BeforeAfterGallery, homepage section ordering updated.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-conversion-and-design-polish/09-CONTEXT.md
@.planning/phases/09-conversion-and-design-polish/09-RESEARCH.md
@.planning/phases/09-conversion-and-design-polish/09-01-SUMMARY.md

@src/data/site-config.ts
@src/data/testimonials.ts
@src/components/sections/TrustBar.tsx
@src/components/sections/TestimonialCarousel.tsx
@src/app/page.tsx
@src/components/templates/ServiceTemplate.tsx
@src/components/templates/CityTemplate.tsx

<interfaces>
From src/components/animations/CountUp.tsx (created in Plan 01):
```typescript
export function CountUp({ target, suffix, prefix, duration }: {
  target: number; suffix?: string; prefix?: string; duration?: number;
}): JSX.Element;
```

From src/components/animations/AnimateIn.tsx (created in Plan 01):
```typescript
export function AnimateIn({ children, className, delay }: {
  children: ReactNode; className?: string; delay?: number;
}): JSX.Element;
```

From src/data/testimonials.ts:
```typescript
export interface Testimonial {
  name: string;
  location: string;
  rating: number;
  text: string;
  service: string;
}
```

From src/data/site-config.ts:
```typescript
trustStats: [
  { label: 'Roofs Completed', value: '500+' },
  { label: 'Years Experience', value: '15+' },
  { label: 'Star Rating', value: '5.0' },
  { label: 'Licensed & Insured', value: 'Yes' },
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign TrustBar and enhance TestimonialCarousel</name>
  <files>src/data/site-config.ts, src/components/sections/TrustBar.tsx, src/components/sections/TestimonialCarousel.tsx</files>
  <action>
**1. Update site-config.ts trustStats:**
Add numeric values for count-up animation alongside existing display values. Each stat needs: `label`, `value` (display string), `numericValue` (number), `suffix` (string), `prefix` (string). Map:
- Roofs Completed: numericValue=500, suffix='+', prefix=''
- Years Experience: numericValue=15, suffix='+', prefix=''
- Star Rating: numericValue=5, suffix='.0', prefix='' (note: animate to 5, show "5.0")
- Licensed and Insured: this is a boolean, NOT numeric — display as text "Yes" with a checkmark icon, no count-up

Add an `icon` field to each stat: 'shield' | 'certificate' | 'clock' | 'checkmark' — used for SVG icon selection.

**2. Redesign TrustBar.tsx:**
- Convert to 'use client' (needed for CountUp child component, or keep as server component wrapping a client TrustBarInner)
- REMOVE all Image imports and badge images (GAF, BBB, HomeAdvisor)
- Add props: `variant?: 'full' | 'compact'` (full = homepage, compact = service/city pages — same layout but optionally smaller)
- Layout: horizontal row on desktop (4 items evenly spaced), 2x2 grid on mobile
- Background: parchment-dark with subtle border-y
- Each stat card: custom SVG icon (copper color, 1.5px stroke width) above number + label
- SVG icons (inline, not image files):
  - shield: for "Licensed & Insured"
  - certificate: for "Star Rating"
  - clock: for "Years Experience"
  - checkmark: for "Roofs Completed" (or use a house icon)
- Numbers: font-heading text-3xl font-bold text-forest — rendered via CountUp component (except "Licensed & Insured" which shows "Yes" as text)
- Labels: font-body text-sm text-text-secondary
- Wrap each stat card in AnimateIn for scroll reveal

**3. Enhance TestimonialCarousel.tsx:**
- Add aggregate rating header ABOVE the cards: "4.9 out of 5 — based on 200+ reviews" with 5 star icons
- The aggregate rating section: centered, font-heading text-2xl, star icons at h-6 w-6, "200+ reviews" in font-body text-text-secondary
- REMOVE the review links section entirely (the `<div>` with Google, HomeAdvisor, BBB links at the bottom)
- Keep all existing carousel logic (auto-rotate, dot indicators, desktop 3-card/mobile 1-card)
- Keep StarRating component as-is
- Keep TestimonialCard as-is
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/Newarkqualityroofing && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>TrustBar shows 4 text stat cards with SVG icons and CountUp animation, no badge images. TestimonialCarousel has aggregate rating header "4.9 out of 5 — based on 200+ reviews" above cards. Google/HomeAdvisor/BBB review links removed. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create BeforeAfterGallery and CompactTestimonial, wire into pages</name>
  <files>src/components/sections/BeforeAfterGallery.tsx, src/components/sections/CompactTestimonial.tsx, src/app/page.tsx, src/components/templates/ServiceTemplate.tsx, src/components/templates/CityTemplate.tsx</files>
  <action>
**1. Create BeforeAfterGallery.tsx:**
- 'use client' directive
- Custom slider component using CSS clip-path with pointer event tracking
- Data: array of 4 gallery projects defined inline (not a separate data file):
  1. Residential shingle repair: before=`/images/residential-roof-repair-newark.jpg`, after=`/images/newark-roofing-at-work.jpg`, caption="Residential Shingle Repair — Newark, NJ"
  2. Commercial flat roof install: before=`/images/commercial-roofing-newark.jpg`, after=`/images/newark-roofing-team.jpg`, caption="Commercial Flat Roof Installation — East Orange, NJ"
  3. Residential full replacement: before=`/images/homeowner-roofing-options.jpg`, after=`/images/roof-replacement-newark.jpg`, caption="Full Roof Replacement — Montclair, NJ"
  4. Commercial TPO membrane: before=`/images/essex-county-experience.jpg`, after=`/images/manufacturer-warranty.jpg`, caption="Commercial TPO Membrane — Bloomfield, NJ"
- Each slider item:
  - aspect-[3/2] container with `photo-treatment` class
  - After image: full width, absolute positioned
  - Before image: absolute, clipped via `clipPath: inset(0 ${100 - position}% 0 0)`
  - Drag handle: vertical line in copper with circular grab indicator at center
  - Uses onPointerDown/Move/Up for unified mouse+touch control
  - Sets `touch-action: none` on container, `pointer-events: none` on images
  - ARIA: role="slider", aria-label, aria-valuemin/max/now
- Layout: 2-column grid on desktop (lg:grid-cols-2), single column on mobile, gap-6
- Section heading: "Our Recent Projects" in font-heading text-3xl font-bold text-forest
- Below heading: "Drag the slider to see before and after" in font-body text-text-secondary
- Each slider has caption below: location + service type in font-body text-sm text-text-secondary
- Wrap section in AnimateIn

- **ImageObject JSON-LD:** Create a `<script type="application/ld+json">` tag in the gallery section with an array of ImageObject schemas for each gallery image. Use the buildImageObjectSchema pattern from RESEARCH.md. Include contentUrl (full URL with SEO_CONFIG.BASE_URL prefix), name (alt text), caption, width, height.

**2. Create CompactTestimonial.tsx:**
- Server component (no interactivity needed)
- Props: `testimonials: Testimonial[]`, `filterBy: { type: 'service' | 'location'; value: string }`
- Logic: Filter testimonials array by filterBy.type matching filterBy.value (case-insensitive partial match). Show first 2 matches. If fewer than 2 matches, fill with first available testimonials as fallback.
- Layout: vertical stack of 1-2 cards on parchment-light background
- Each card: rounded-lg border border-border bg-white p-5, star rating, quote, name/location/service
- Section heading: "What Our Customers Say" in font-heading text-xl font-semibold text-forest
- Keep styling consistent with TestimonialCard in carousel but slightly more compact (p-5 instead of p-6, text-sm body)
- Import and reuse the StarRating component — extract it from TestimonialCarousel into a shared location OR duplicate it (simpler since it is stateless)

**3. Wire BeforeAfterGallery into homepage page.tsx:**
- Import BeforeAfterGallery
- Insert between `<HomeResidentialCommercial />` and `<TestimonialCarousel />` (per CONTEXT.md decision)
- Pass no props (data is internal to component)

**4. Wire CompactTestimonial into ServiceTemplate.tsx and CityTemplate.tsx:**
- Import CompactTestimonial and testimonials data
- In ServiceTemplate: add CompactTestimonial after the FAQ section, before the CTA/related section. Filter by service name.
- In CityTemplate: add CompactTestimonial after the FAQ section, before the nearby cities section. Filter by location (city name).
- Both: pass the full testimonials array and appropriate filterBy prop

**Trust bar placement per CONTEXT.md decision:**
- Trust bar is ALREADY on the homepage (page.tsx)
- Add TrustBar import and render in ServiceTemplate.tsx (after hero, before content) and CityTemplate.tsx (after hero, before content)
- Do NOT add trust bar to combo/article/comparison pages
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/Newarkqualityroofing && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>BeforeAfterGallery renders 4 slider items on homepage between ResidentialCommercial and TestimonialCarousel. CompactTestimonial renders 1-2 relevant testimonials on service and city pages. TrustBar appears on homepage, service pages, and city pages. ImageObject JSON-LD present on gallery items. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- TrustBar.tsx has no `<Image>` imports for badges (grep confirms no gaf-certified, bbb-accredited, homeadvisor references)
- TrustBar.tsx imports CountUp from animations
- TestimonialCarousel.tsx contains "4.9 out of 5" aggregate rating text
- TestimonialCarousel.tsx has NO `<Link>` to google.com, homeadvisor.com, or bbb.org
- BeforeAfterGallery.tsx uses onPointerDown/Move/Up and clipPath
- BeforeAfterGallery.tsx has photo-treatment class on slider containers
- page.tsx imports BeforeAfterGallery
- ServiceTemplate.tsx and CityTemplate.tsx import CompactTestimonial and TrustBar
- `npm run seo:validate` passes
</verification>

<success_criteria>
Trust bar redesigned as text-only with SVG icons and count-up animation on 3 numeric stats. Badge images completely removed. Testimonial carousel has aggregate rating header and no outbound review links. Before/after gallery displays 4 draggable slider comparisons on homepage with CSS brand treatment and ImageObject JSON-LD. Compact testimonial variant renders on service and city template pages. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-conversion-and-design-polish/09-02-SUMMARY.md`
</output>
