---
phase: 02-lead-capture-core-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/actions/submit-lead.ts
  - src/components/forms/LeadForm.tsx
  - src/components/forms/SubmitButton.tsx
  - src/components/ui/PhoneNumber.tsx
  - src/data/site-config.ts
  - src/data/testimonials.ts
  - src/data/faq.ts
  - src/lib/schemas.ts
  - .env.example
autonomous: true

must_haves:
  truths:
    - "Lead form validates 4 fields (name, email, phone, service) with inline error messages"
    - "Form submits server-side to GoHighLevel webhook and logs every submission"
    - "Phone number component renders as a clickable tel: link with icon"
    - "Service dropdown groups all 63 services by category using optgroup"
  artifacts:
    - path: "src/app/actions/submit-lead.ts"
      provides: "Server Action for lead form submission with Zod validation, GHL webhook proxy, server-side logging, redirect to /thank-you"
      contains: "use server"
    - path: "src/components/forms/LeadForm.tsx"
      provides: "Reusable client component with useActionState, 4 fields, service dropdown with optgroup, context-aware defaultService/defaultCity props"
      contains: "use client"
    - path: "src/components/forms/SubmitButton.tsx"
      provides: "Submit button using useFormStatus for pending state"
      contains: "useFormStatus"
    - path: "src/components/ui/PhoneNumber.tsx"
      provides: "Click-to-call component reading from env vars with phone icon SVG"
      contains: "tel:"
    - path: "src/data/site-config.ts"
      provides: "Centralized business info: company name, address, phone, trust stats, business hours"
    - path: "src/data/testimonials.ts"
      provides: "Array of 6 placeholder testimonials with name, location, rating, text"
    - path: "src/data/faq.ts"
      provides: "Array of 8-10 roofing FAQ items with question and answer strings"
    - path: ".env.example"
      provides: "Template for GHL_WEBHOOK_URL, NEXT_PUBLIC_PHONE_DISPLAY, NEXT_PUBLIC_PHONE_TEL"
  key_links:
    - from: "src/components/forms/LeadForm.tsx"
      to: "src/app/actions/submit-lead.ts"
      via: "useActionState(submitLead, initialState)"
      pattern: "useActionState.*submitLead"
    - from: "src/app/actions/submit-lead.ts"
      to: "GoHighLevel webhook"
      via: "fetch(process.env.GHL_WEBHOOK_URL)"
      pattern: "GHL_WEBHOOK_URL"
    - from: "src/components/forms/LeadForm.tsx"
      to: "src/data/nav-data.ts"
      via: "getServiceMenuGroups() for optgroup population"
      pattern: "optgroup"
---

<objective>
Build the lead capture foundation: Server Action, LeadForm component, PhoneNumber component, and all supporting data files (site config, testimonials, FAQ).

Purpose: Every subsequent plan in Phase 2 depends on these components. The LeadForm and PhoneNumber are reused on homepage, contact, header, and eventually all service/city pages.
Output: Reusable form and phone components, server action with GHL webhook proxy, data files for homepage sections.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-lead-capture-core-pages/02-RESEARCH.md
@.planning/phases/02-lead-capture-core-pages/02-CONTEXT.md

@src/lib/schemas.ts
@src/lib/types.ts
@src/data/services.ts
@src/data/nav-data.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Action, Lead Form Schema, and Data Files</name>
  <files>
    src/app/actions/submit-lead.ts
    src/lib/schemas.ts
    src/data/site-config.ts
    src/data/testimonials.ts
    src/data/faq.ts
    .env.example
  </files>
  <action>
    1. Add `LeadFormSchema` to `src/lib/schemas.ts`:
       - name: z.string().min(2, 'Name is required')
       - email: z.string().email('Valid email is required')
       - phone: z.string().min(10, 'Valid phone number is required')
       - serviceNeeded: z.string().min(1, 'Please select a service')

    2. Create `src/app/actions/submit-lead.ts` as a Server Action ('use server'):
       - Export `LeadFormState` type with `errors?` (per-field string arrays), `message?`, `success?`
       - Export `submitLead(prevState: LeadFormState, formData: FormData): Promise<LeadFormState>`
       - Validate with LeadFormSchema.safeParse, return flattened field errors on failure
       - ALWAYS log `console.log('[LEAD]', JSON.stringify({ ...payload, timestamp: new Date().toISOString() }))` regardless of webhook success
       - Wrap GHL webhook fetch in try/catch. Use `let shouldRedirect = false` pattern to avoid redirect() inside try/catch (redirect throws internally). Set shouldRedirect = true after successful log (even if webhook fails). Call `redirect('/thank-you')` AFTER the try/catch block.
       - If `process.env.GHL_WEBHOOK_URL` is missing, log a warning `[LEAD_NO_WEBHOOK]` but still redirect (lead is logged)
       - GHL payload format: `{ firstName: name, email, phone, tags: [serviceNeeded], source: 'website-lead-form' }`

    3. Create `src/data/site-config.ts`:
       - Export `siteConfig` object with: companyName, phone (display + tel from env with fallbacks), address (street, city, state, zip -- placeholder), email (placeholder), businessHours (Mon-Fri 7AM-6PM, Sat 8AM-2PM, Sun Emergency Only), trustStats array: [{label: 'Roofs Completed', value: '500+'}, {label: 'Years Experience', value: '15+'}, {label: 'Star Rating', value: '5.0'}, {label: 'Licensed & Insured', value: 'Yes'}]
       - Phone fallback values: display "(973) 555-0123", tel "+19735550123"
       - Use `process.env.NEXT_PUBLIC_PHONE_DISPLAY` and `process.env.NEXT_PUBLIC_PHONE_TEL` with fallbacks

    4. Create `src/data/testimonials.ts`:
       - Export `Testimonial` interface: { name: string, location: string, rating: number, text: string, service: string }
       - Export `testimonials` array with 6 realistic placeholder testimonials from different Essex County cities, mentioning specific services (roof repair, shingle replacement, etc.), ratings 4.5-5.0, text 2-3 sentences each

    5. Create `src/data/faq.ts`:
       - Export `FaqItem` interface: { question: string, answer: string }
       - Export `faqItems` array with 8 roofing FAQ items: cost estimates, insurance claims, timeline, emergency service, warranty, signs you need a new roof, commercial vs residential differences, free estimates

    6. Create `.env.example`:
       ```
       # GoHighLevel webhook URL (from GHL Workflows > Inbound Webhook trigger)
       GHL_WEBHOOK_URL=

       # Phone number (GoHighLevel tracking number)
       NEXT_PUBLIC_PHONE_DISPLAY=(973) 555-0123
       NEXT_PUBLIC_PHONE_TEL=+19735550123

       # Google Maps embed (optional)
       NEXT_PUBLIC_GOOGLE_MAPS_EMBED_KEY=
       ```
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no errors
    - `src/app/actions/submit-lead.ts` contains 'use server', 'redirect', '[LEAD]', 'GHL_WEBHOOK_URL', 'shouldRedirect'
    - `src/data/site-config.ts` exports siteConfig with phone, address, trustStats
    - `src/data/testimonials.ts` exports 6 testimonials
    - `src/data/faq.ts` exports 8 FAQ items
  </verify>
  <done>Server Action validates, logs, proxies to GHL, and redirects. All data files export typed, populated arrays.</done>
</task>

<task type="auto">
  <name>Task 2: LeadForm, SubmitButton, and PhoneNumber Components</name>
  <files>
    src/components/forms/LeadForm.tsx
    src/components/forms/SubmitButton.tsx
    src/components/ui/PhoneNumber.tsx
  </files>
  <action>
    1. Create `src/components/forms/SubmitButton.tsx` ('use client'):
       - Import `useFormStatus` from 'react-dom'
       - Render a button with text "Get My Free Estimate" (per CONTEXT.md)
       - When pending: show "Submitting..." with disabled state and reduced opacity
       - Style: full-width, bg-copper, text-text-on-copper, font-heading font-bold, py-3, rounded-md, hover:bg-copper-dark, transition, focus-visible ring

    2. Create `src/components/forms/LeadForm.tsx` ('use client'):
       - Props: `{ defaultService?: string, defaultCity?: string, className?: string, variant?: 'hero' | 'standard' }`
       - Use `useActionState(submitLead, initialState)` from 'react'
       - Headline: "Get Your Free Roofing Estimate" in font-heading text-2xl font-bold
       - 4 fields in a vertical stack:
         a. Full Name: text input, autoComplete="name", required
         b. Email: email input, autoComplete="email", required
         c. Phone: tel input, autoComplete="tel", required
         d. Service Needed: select with optgroup elements

       - For the service dropdown: Import `getServiceMenuGroups` from '@/data/nav-data'. Since this is 'use client', the parent server component should pass serviceGroups as a prop. Add `serviceGroups` to props interface. Map groups into `<optgroup label={group.categoryLabel}>` with `<option value={service.slug}>` for each service. Set defaultValue from `defaultService` prop.

       - Each field has a `<label>` wrapping or linked via htmlFor, with the input below
       - Error display: Below each input, if `state.errors?.fieldName`, show `<p className="text-red-600 text-sm mt-1" aria-live="polite">{errors[0]}</p>`
       - Use SubmitButton at bottom

       - Styling (variant='hero'): Semi-transparent dark background (bg-forest-dark/90), white/parchment text, rounded-lg, p-6-8, backdrop-blur for glass effect
       - Styling (variant='standard'): bg-parchment-light, border border-border, text-text-primary, rounded-lg, p-6
       - Input styling: w-full, rounded-md, border border-border, bg-white, px-4 py-3, font-body, focus:ring-2 focus:ring-copper focus:border-copper
       - Select styling: Same as inputs with appearance-none and custom chevron via background-image or trailing SVG

    3. Create `src/components/ui/PhoneNumber.tsx` (server component, NOT 'use client'):
       - Import siteConfig from '@/data/site-config'
       - Props: `{ className?: string, showIcon?: boolean, size?: 'sm' | 'md' | 'lg' }`
       - Render `<a href="tel:{siteConfig.phone.tel}">` with inline SVG phone icon (when showIcon=true, default true) + display number
       - Size variants: sm (text-sm), md (text-base), lg (text-lg font-semibold)
       - Default: copper text color, hover:text-copper-dark
       - aria-label="Call us at {displayNumber}"
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - LeadForm.tsx contains 'use client', 'useActionState', 'submitLead', 'optgroup', 'aria-live'
    - SubmitButton.tsx contains 'useFormStatus', 'Get My Free Estimate'
    - PhoneNumber.tsx contains 'tel:', 'aria-label'
    - `npm run build` succeeds (full build validates everything wires together)
  </verify>
  <done>LeadForm renders 4 fields with category-grouped service dropdown, inline validation errors, and pending state. PhoneNumber renders clickable tel: link. Both are reusable across all pages.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Server Action file contains structured logging with [LEAD] prefix
- LeadForm accepts defaultService and defaultCity props for context-aware pre-filling
- PhoneNumber reads from env vars with sensible fallbacks
- All 63 services appear in the dropdown grouped by 8 categories
</verification>

<success_criteria>
- LeadForm component is importable and renders a complete 4-field form with validation
- Server Action validates, logs, proxies to GHL webhook, and redirects to /thank-you
- PhoneNumber component renders clickable phone link
- site-config.ts, testimonials.ts, and faq.ts export populated data
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-lead-capture-core-pages/02-01-SUMMARY.md`
</output>
