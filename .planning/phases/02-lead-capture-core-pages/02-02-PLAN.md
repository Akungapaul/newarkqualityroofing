---
phase: 02-lead-capture-core-pages
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/page.tsx
  - src/components/sections/HeroSection.tsx
  - src/components/sections/TrustBar.tsx
  - src/components/sections/ServicesGrid.tsx
  - src/components/sections/TestimonialCarousel.tsx
  - src/components/sections/FaqAccordion.tsx
  - src/components/sections/LocationsGrid.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage displays hero with lead form overlaid on a dramatic full-bleed background"
    - "Trust bar shows credentials and stats immediately below the hero"
    - "Services grid shows photo-card style links grouped by residential and commercial"
    - "Testimonial carousel auto-rotates with star ratings and respects prefers-reduced-motion"
    - "Locations grid links to all 21 Essex County city pages"
    - "FAQ accordion uses native details/summary with no JavaScript"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage assembling all sections in conversion-optimized order"
      min_lines: 30
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Full-bleed hero with CSS gradient/pattern background and LeadForm overlaid"
    - path: "src/components/sections/TrustBar.tsx"
      provides: "Horizontal stats bar from siteConfig.trustStats"
    - path: "src/components/sections/ServicesGrid.tsx"
      provides: "Photo card grid split into residential and commercial sections"
    - path: "src/components/sections/TestimonialCarousel.tsx"
      provides: "Auto-rotating testimonial cards with star ratings"
      contains: "use client"
    - path: "src/components/sections/FaqAccordion.tsx"
      provides: "Collapsible Q&A using details/summary HTML elements"
      contains: "details"
    - path: "src/components/sections/LocationsGrid.tsx"
      provides: "Grid of city cards linking to city pages"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/sections/HeroSection.tsx"
      via: "Import and render as first section"
      pattern: "HeroSection"
    - from: "src/components/sections/HeroSection.tsx"
      to: "src/components/forms/LeadForm.tsx"
      via: "LeadForm embedded in hero with variant='hero'"
      pattern: "LeadForm.*hero"
    - from: "src/components/sections/ServicesGrid.tsx"
      to: "src/data/services.ts"
      via: "Import services, filter by isResidential/isCommercial"
      pattern: "isResidential|isCommercial"
    - from: "src/components/sections/LocationsGrid.tsx"
      to: "src/data/cities.ts"
      via: "Import cities, generate slugs with generateCityPageSlug"
      pattern: "generateCityPageSlug"
---

<objective>
Build the complete homepage with all 7 sections in conversion-optimized order: Hero + form, Trust bar, Services grid, Testimonials carousel, Locations grid, FAQ accordion, final CTA.

Purpose: The homepage is the primary landing page and conversion driver. It needs to immediately present the lead form above the fold, establish credibility, and showcase services/locations.
Output: Fully rendered homepage with all sections using real data from Phase 1 + data files from Plan 01.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-lead-capture-core-pages/02-CONTEXT.md
@.planning/phases/02-lead-capture-core-pages/02-RESEARCH.md

@src/app/globals.css
@src/data/services.ts
@src/data/cities.ts
@src/data/nav-data.ts
@src/lib/schemas.ts

Read 02-01-SUMMARY.md for component APIs and data file shapes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Homepage Section Components</name>
  <files>
    src/components/sections/HeroSection.tsx
    src/components/sections/TrustBar.tsx
    src/components/sections/ServicesGrid.tsx
    src/components/sections/TestimonialCarousel.tsx
    src/components/sections/FaqAccordion.tsx
    src/components/sections/LocationsGrid.tsx
  </files>
  <action>
    1. **HeroSection.tsx** (server component wrapper):
       - Full viewport height (min-h-[85vh]) section with a dramatic CSS background: layered gradient from forest-dark to forest with a subtle geometric pattern or grain texture overlay (CSS only, no images needed for v1). Use radial gradients, linear gradients, and/or CSS noise patterns for depth.
       - Two-column layout on desktop (lg:grid-cols-2): left side has headline + subtitle, right side has LeadForm
       - Left content: H1 "Expert Roofing for Essex County" (or similar), subtitle paragraph about licensed/insured quality, and a "or call us" link with PhoneNumber component
       - Right content: LeadForm with variant="hero", pass serviceGroups from getServiceMenuGroups()
       - Mobile: stack vertically, form below headline
       - The form should have id="lead-form" for the header CTA anchor link

    2. **TrustBar.tsx** (server component):
       - Import siteConfig.trustStats
       - Horizontal bar with bg-parchment-dark, py-6
       - Display stats in a flex row (4 items), each with large value text (font-heading text-3xl font-bold text-forest) and label below (font-body text-sm text-text-secondary)
       - Add a subtle top/bottom border
       - On mobile: 2x2 grid
       - Include BBB Accredited badge placeholder (text-only)
       - Full trust text: "500+ Roofs Completed | Licensed | Insured | 5-Star Rated | BBB Accredited" -- distribute across the stat items

    3. **ServicesGrid.tsx** (server component):
       - Import services from '@/data/services'
       - Section heading: "Our Roofing Services" with residential/commercial subheadings
       - Two subsections: "Residential Services" filtering services where isResidential=true, "Commercial Services" filtering where isCommercial=true
       - Display as photo-card style grid (3 cols desktop, 2 tablet, 1 mobile)
       - Each card: CSS gradient background simulating a photo card (dark overlay gradient), service name as card title, shortDescription as card text, Link to /{service.slug}
       - Group by category but show only 2-3 representative services per category (not all 63). Show the first service from each category for each section, totaling ~8 residential + ~6 commercial cards with a "View All Services" link to /services
       - Hover: slight scale transform, shadow increase

    4. **TestimonialCarousel.tsx** ('use client'):
       - Import testimonials array (pass as prop from server parent to avoid importing data in client component)
       - Props: `{ testimonials: Testimonial[] }`
       - Auto-rotate every 5 seconds, respect prefers-reduced-motion (disable auto-rotate)
       - Show 1 card at a time on mobile, 3 cards on desktop (visible simultaneously)
       - Each card: star rating (render N filled stars + empty stars using SVG), quoted text in italic font-body, name and location below
       - Navigation: dot indicators at bottom, clickable to jump to specific testimonial
       - Card styling: bg-white, rounded-lg, shadow-md, p-6, border border-border
       - Section wrapper with heading "What Our Customers Say" and bg-parchment-light
       - Wrap in `role="region" aria-label="Customer testimonials"` with `aria-live="polite"`

    5. **FaqAccordion.tsx** (server component -- NO 'use client'):
       - Import faqItems (pass as prop from server parent)
       - Props: `{ items: FaqItem[] }`
       - Use native `<details>` and `<summary>` elements
       - Each item: border-b border-border, summary has cursor-pointer, py-4, font-heading text-lg font-semibold text-forest
       - Chevron icon that rotates on open using `group-open:rotate-180` with Tailwind group class
       - Answer text: pb-4 font-body text-text-secondary leading-relaxed
       - Section wrapper: max-w-3xl mx-auto, heading "Frequently Asked Questions"

    6. **LocationsGrid.tsx** (server component):
       - Import cities from '@/data/cities' and generateCityPageSlug from '@/lib/slug-utils'
       - Grid of city cards (3 cols desktop, 2 tablet, 1 mobile)
       - Each card: Link to /{generateCityPageSlug(city.slug)}, city name, "NJ" label, subtle hover effect
       - Mark Newark with a special "Headquarters" badge (check city.isHQ)
       - Section heading: "Serving All of Essex County"
       - bg-forest-dark text-text-on-dark section for visual contrast
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - All 6 component files exist in src/components/sections/
    - TestimonialCarousel.tsx contains 'use client', 'prefers-reduced-motion', 'aria-live'
    - FaqAccordion.tsx does NOT contain 'use client'
    - FaqAccordion.tsx contains '<details' and '<summary'
    - ServicesGrid.tsx contains 'isResidential' and 'isCommercial'
    - LocationsGrid.tsx contains 'generateCityPageSlug'
  </verify>
  <done>All 6 homepage section components render with real data, proper accessibility, and responsive layouts.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble Homepage</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
    Replace the current placeholder homepage with the full conversion-optimized layout.

    1. Make page.tsx a server component (no 'use client')
    2. Import all section components + data:
       - getServiceMenuGroups from '@/data/nav-data'
       - testimonials from '@/data/testimonials'
       - faqItems from '@/data/faq'
       - siteConfig from '@/data/site-config'

    3. Call getServiceMenuGroups() at the top level to get serviceGroups, pass to HeroSection

    4. Render sections in this order (per CONTEXT.md conversion-optimized ordering):
       a. HeroSection (with serviceGroups prop)
       b. TrustBar
       c. ServicesGrid
       d. TestimonialCarousel (pass testimonials as prop)
       e. LocationsGrid
       f. FaqAccordion (pass faqItems as prop)
       g. Final CTA section: simple bg-copper section with "Ready for Your Free Estimate?" heading, brief text, and two buttons: one linking to #lead-form (scroll to top), one PhoneNumber component. Keep this simple, ~20 lines.

    5. Each section should have appropriate vertical spacing (py-16 lg:py-24) and section semantic element

    6. Export metadata for SEO:
       ```
       export const metadata = {
         title: 'Newark Quality Roofing | Expert Roofing in Essex County NJ',
         description: 'Licensed, insured roofing contractor serving Newark and Essex County NJ. Residential & commercial roof repair, replacement, and installation. Free estimates.',
       }
       ```
  </action>
  <verify>
    - `npm run build` succeeds
    - src/app/page.tsx imports all 6 section components
    - Page renders sections in order: Hero, Trust, Services, Testimonials, Locations, FAQ, CTA
    - Page exports metadata object
    - `npm run dev` and visiting http://localhost:3000 shows full homepage (visual check during dev)
  </verify>
  <done>Homepage renders all 7 sections with real data, lead form above the fold, conversion-optimized ordering, and proper SEO metadata.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Homepage at / renders Hero with embedded LeadForm, TrustBar, ServicesGrid (residential + commercial), TestimonialCarousel, LocationsGrid (21 cities), FaqAccordion, and final CTA
- Lead form is above the fold in the hero section
- Testimonial carousel auto-rotates (if motion not reduced)
- FAQ accordion expands/collapses without JavaScript
- All links to services and cities use correct slug patterns
</verification>

<success_criteria>
- Full homepage with 7 distinct sections visible
- Lead form embedded in hero section above the fold
- Services grid shows residential and commercial split
- All 21 cities linked in locations grid
- Page builds successfully as static page
</success_criteria>

<output>
After completion, create `.planning/phases/02-lead-capture-core-pages/02-02-SUMMARY.md`
</output>
