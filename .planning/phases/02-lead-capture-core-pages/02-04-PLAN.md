---
phase: 02-lead-capture-core-pages
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/templates/CoreTemplate.tsx
  - src/components/pages/AboutPage.tsx
  - src/components/pages/ContactPage.tsx
autonomous: true

must_haves:
  truths:
    - "About page displays company story, values, certifications, and team with community-focused tone"
    - "Contact page shows lead form, Google Maps embed, phone number, and business address"
    - "CoreTemplate dispatches to page-specific components based on corePage.id"
  artifacts:
    - path: "src/components/pages/AboutPage.tsx"
      provides: "About page with credentials, community roots, team section (placeholder content)"
    - path: "src/components/pages/ContactPage.tsx"
      provides: "Contact page with LeadForm, Google Maps iframe, NAP info, business hours"
      contains: "iframe"
    - path: "src/components/templates/CoreTemplate.tsx"
      provides: "Updated dispatcher routing corePage.id to specific page components"
  key_links:
    - from: "src/components/templates/CoreTemplate.tsx"
      to: "src/components/pages/AboutPage.tsx"
      via: "Switch on corePage.id === 'about'"
      pattern: "about.*AboutPage|AboutPage.*about"
    - from: "src/components/pages/ContactPage.tsx"
      to: "src/components/forms/LeadForm.tsx"
      via: "Embedded LeadForm with variant='standard'"
      pattern: "LeadForm"
    - from: "src/components/pages/ContactPage.tsx"
      to: "src/data/site-config.ts"
      via: "Import siteConfig for address, phone, hours"
      pattern: "siteConfig"
---

<objective>
Build the About and Contact pages with full content, and update CoreTemplate to dispatch to page-specific components.

Purpose: About and Contact are high-trust core pages that reinforce credibility and provide direct contact methods. They have unique layouts (Contact has map + form, About has story + team) that justify dedicated components.
Output: Fully rendered About and Contact pages accessible at /about and /contact.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-lead-capture-core-pages/02-CONTEXT.md
@.planning/phases/02-lead-capture-core-pages/02-RESEARCH.md

@src/components/templates/CoreTemplate.tsx
@src/data/core-pages.ts
@src/app/globals.css

Read 02-01-SUMMARY.md for LeadForm and PhoneNumber component APIs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: About and Contact Page Components</name>
  <files>
    src/components/pages/AboutPage.tsx
    src/components/pages/ContactPage.tsx
  </files>
  <action>
    1. **AboutPage.tsx** (server component):
       Per CONTEXT.md: "Professional credentials + community-focused tone -- certifications, team expertise, insurance alongside local roots and community involvement."

       Sections (top to bottom):
       a. **Hero banner**: bg-forest, H1 "About Newark Quality Roofing", subtitle about serving Essex County for 15+ years
       b. **Our Story**: 2-3 paragraphs about founding, growth, commitment to quality. Placeholder content but written in a compelling, authentic tone. Mention Newark roots, family-owned, community involvement.
       c. **Our Values**: 3-4 value cards in a grid (Quality Craftsmanship, Customer First, Community Roots, Honest Pricing). Each with an icon (simple SVG), title, and 2-sentence description.
       d. **Credentials & Certifications**: List of credential items: Licensed NJ Roofing Contractor, Fully Insured (General Liability + Workers Comp), GAF Certified Installer (placeholder), BBB Accredited (placeholder), OSHA Safety Certified. Display as a clean list with checkmark icons.
       e. **Our Team**: 3 placeholder team member cards (Owner/CEO, Senior Project Manager, Lead Installer) with placeholder initials avatar (no photos), name, title, brief bio. Note: "Photos coming soon" subtle text.
       f. **Community Involvement**: Short section about sponsoring local events, supporting Newark schools, etc. Placeholder but realistic content.
       g. **CTA**: "Ready to work with a team you can trust?" with link to /contact and PhoneNumber component

       Overall: max-w-5xl mx-auto, generous vertical spacing between sections, alternating bg colors (parchment / parchment-light)

    2. **ContactPage.tsx** (server component):
       Per CONTEXT.md: "Lead form + phone number + business address. Google Maps embed. Placeholder address."

       Layout: Two-column on desktop (lg:grid-cols-5), single column mobile.

       Left column (lg:col-span-3):
       a. H1: "Contact Newark Quality Roofing"
       b. Subtitle: "Get your free roofing estimate today"
       c. LeadForm component with variant="standard", pass serviceGroups (import getServiceMenuGroups)
       d. Note below form: "We respond to all inquiries within 24 hours"

       Right column (lg:col-span-2):
       a. **Contact Info card**: bg-forest text-text-on-dark rounded-lg p-6
          - PhoneNumber (size='lg', light color variant)
          - Email: info@newarkqualityroofing.com (placeholder)
          - Address from siteConfig (street, city, state, zip)
          - Business hours from siteConfig
       b. **Emergency banner**: Small callout: "24/7 Emergency Service Available" with phone link

       Below columns (full width):
       c. **Google Maps embed**: Use keyless embed URL approach:
          `<iframe src="https://maps.google.com/maps?q=Newark+NJ&t=&z=13&ie=UTF8&iwloc=&output=embed" width="100%" height="400" style={{ border: 0 }} allowFullScreen loading="lazy" title="Newark Quality Roofing location" />`
          - Set explicit width="100%" and height="400" to prevent layout shift
          - loading="lazy" per research pitfall #5
          - Rounded corners with overflow-hidden wrapper
  </action>
  <verify>
    - Both files exist in src/components/pages/
    - `npx tsc --noEmit` passes
    - ContactPage.tsx contains 'iframe', 'loading="lazy"', 'LeadForm', 'siteConfig'
    - AboutPage.tsx contains credential/certification content
  </verify>
  <done>About page shows company story, values, credentials, and team. Contact page shows form, map, and full contact info.</done>
</task>

<task type="auto">
  <name>Task 2: Update CoreTemplate Dispatcher</name>
  <files>
    src/components/templates/CoreTemplate.tsx
  </files>
  <action>
    Update CoreTemplate to dispatch to page-specific components based on corePage.id.

    1. Import AboutPage and ContactPage from '@/components/pages/'
    2. Add a switch/if-else on corePage.id:
       - 'about' -> render `<AboutPage />`
       - 'contact' -> render `<ContactPage />`
       - 'services' -> keep placeholder (built in Plan 05)
       - 'locations' -> keep placeholder (built in Plan 05)
       - 'html-sitemap' -> keep placeholder (built in Plan 05)
       - 'privacy-policy' -> keep placeholder (built in Plan 05)
       - 'thank-you' -> This won't be reached (dedicated route at /thank-you takes precedence), but keep a simple fallback just in case
       - default -> render existing placeholder template

    3. Pass corePage prop through to sub-components so they can access metadata if needed

    4. Keep the existing placeholder as the default case so pages not yet built (services hub, locations hub, sitemap, privacy) still render something

    Note: The [slug] catch-all dispatches to CoreTemplate for all core pages. The dedicated /thank-you route takes precedence over the catch-all for that specific slug.
  </action>
  <verify>
    - `npm run build` succeeds
    - CoreTemplate.tsx imports AboutPage and ContactPage
    - Visiting /about renders the full About page (not the placeholder)
    - Visiting /contact renders the full Contact page with form and map
    - Other core pages (services, locations, etc.) still render the placeholder
  </verify>
  <done>CoreTemplate routes to page-specific components for about and contact, with placeholders for remaining pages.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- /about renders company story, values, credentials, team sections
- /contact renders lead form, contact info card, Google Maps embed
- /contact map iframe has loading="lazy"
- Other core pages still render (placeholder is fine)
</verification>

<success_criteria>
- About page accessible at /about with full content sections
- Contact page accessible at /contact with form, map, and NAP info
- CoreTemplate correctly dispatches based on corePage.id
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-lead-capture-core-pages/02-04-SUMMARY.md`
</output>
