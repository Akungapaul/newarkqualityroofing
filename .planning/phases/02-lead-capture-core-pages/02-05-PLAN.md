---
phase: 02-lead-capture-core-pages
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/pages/ServicesHubPage.tsx
  - src/components/pages/LocationsHubPage.tsx
  - src/components/pages/SitemapPage.tsx
  - src/components/pages/PrivacyPolicyPage.tsx
  - src/components/templates/CoreTemplate.tsx
autonomous: true

must_haves:
  truths:
    - "Services hub page lists all 63 services grouped by category with residential/commercial split"
    - "Locations hub page lists all 21 Essex County cities in a grid"
    - "HTML sitemap page lists all URLs organized by category for crawlability"
    - "Privacy policy page displays standard privacy policy text"
    - "All 4 pages are accessible via their slugs and render complete content"
  artifacts:
    - path: "src/components/pages/ServicesHubPage.tsx"
      provides: "All 63 services listed with residential/commercial split, then category grouping"
      contains: "isResidential"
    - path: "src/components/pages/LocationsHubPage.tsx"
      provides: "Grid of 21 city cards with links to city pages"
      contains: "generateCityPageSlug"
    - path: "src/components/pages/SitemapPage.tsx"
      provides: "All URLs organized by type: core, services, cities, combos, comparisons"
      contains: "slugRegistry"
    - path: "src/components/pages/PrivacyPolicyPage.tsx"
      provides: "Standard privacy policy adapted for roofing contractor"
    - path: "src/components/templates/CoreTemplate.tsx"
      provides: "Updated dispatcher with all 4 new page routes added"
  key_links:
    - from: "src/components/pages/ServicesHubPage.tsx"
      to: "src/data/services.ts"
      via: "Import services, group by category, split by residential/commercial"
      pattern: "services.*category|isResidential"
    - from: "src/components/pages/SitemapPage.tsx"
      to: "src/data/slug-registry.ts"
      via: "Import slug registry to list all URLs"
      pattern: "slugRegistry|slug-registry"
    - from: "src/components/templates/CoreTemplate.tsx"
      to: "src/components/pages/*"
      via: "Switch dispatch for services, locations, html-sitemap, privacy-policy IDs"
      pattern: "ServicesHubPage|LocationsHubPage|SitemapPage|PrivacyPolicyPage"
---

<objective>
Build the remaining 4 core pages (Services Hub, Locations Hub, HTML Sitemap, Privacy Policy) and complete the CoreTemplate dispatcher.

Purpose: These pages complete the core page set, provide SEO value through hub pages and sitemap, and fulfill legal requirements (privacy policy).
Output: All 8 core pages fully rendered and accessible.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-lead-capture-core-pages/02-CONTEXT.md
@.planning/phases/02-lead-capture-core-pages/02-RESEARCH.md

@src/components/templates/CoreTemplate.tsx
@src/data/services.ts
@src/data/cities.ts
@src/data/slug-registry.ts
@src/data/nav-data.ts
@src/data/core-pages.ts

Read 02-01-SUMMARY.md for LeadForm and PhoneNumber component APIs.
If 02-04-SUMMARY.md exists, read it for CoreTemplate dispatcher pattern.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Services Hub, Locations Hub, and Sitemap Pages</name>
  <files>
    src/components/pages/ServicesHubPage.tsx
    src/components/pages/LocationsHubPage.tsx
    src/components/pages/SitemapPage.tsx
  </files>
  <action>
    1. **ServicesHubPage.tsx** (server component):
       Per CONTEXT.md: "Residential vs Commercial split as two main sections. Under each split, services grouped by category."

       a. H1: "Our Roofing Services" with subtitle about comprehensive residential and commercial services
       b. Two main sections with clear visual separation:

       **Residential Services** section:
       - Heading: "Residential Roofing Services"
       - Filter services where isResidential === true
       - Group filtered services by category using categoryLabels from nav-data pattern
       - For each category group: subheading with category label, then list/grid of service cards
       - Each service card: Link to /{service.slug}, service name, shortDescription, arrow icon
       - Card styling: bg-white border border-border rounded-lg p-4 hover:shadow-md transition

       **Commercial Services** section:
       - Same pattern but filter isCommercial === true
       - Use a different accent (perhaps bg-forest-dark section) to visually distinguish from residential

       c. Bottom CTA: "Not sure what you need?" with link to /contact and PhoneNumber

       Layout: max-w-6xl mx-auto, service cards in 2-3 column grid

    2. **LocationsHubPage.tsx** (server component):
       a. H1: "Roofing Services Across Essex County, NJ"
       b. Subtitle about serving all 21 communities
       c. Grid of city cards (3 cols desktop, 2 tablet, 1 mobile):
          - Each card: Link to /{generateCityPageSlug(city.slug)}
          - City name prominently, "Essex County, NJ" below
          - List 2-3 zip codes from city.zipCodes
          - Newark card gets "Headquarters" badge (city.isHQ)
          - Hover: border-copper transition, slight shadow
       d. Below grid: Brief text about Essex County coverage + PhoneNumber CTA
       e. Card styling: bg-white border border-border rounded-lg p-6 hover:border-copper

    3. **SitemapPage.tsx** (server component):
       a. H1: "Sitemap"
       b. Organize all URLs by type for crawlability:

       Import slugRegistry from '@/data/slug-registry' and services, cities, corePages, comparisons.

       Sections (each with heading):
       - **Main Pages**: List core pages (home, about, contact, services, locations, privacy)
       - **Roofing Services** (63 items): Group by category, list as links
       - **Service Areas** (21 items): List city pages as links
       - **Service Comparisons** (30 items): List comparison pages as links
       - **Service + City Pages** (1,323 items): Group by city, list combos under each city heading. Use details/summary to collapse each city's combo list (too many to show all at once)

       Each item is a proper `<a>` link to the page for crawlability.
       Style: clean, minimal, organized -- this is for search engines and power users.
       Use semantic HTML: `<nav>`, `<ul>`, `<li>`, proper headings hierarchy.
  </action>
  <verify>
    - All 3 files exist in src/components/pages/
    - `npx tsc --noEmit` passes
    - ServicesHubPage.tsx contains 'isResidential' and 'isCommercial' filters
    - LocationsHubPage.tsx contains 'generateCityPageSlug' and 'isHQ'
    - SitemapPage.tsx contains 'slugRegistry' or imports from slug-registry, and links to all page types
  </verify>
  <done>Services hub shows all 63 services with residential/commercial split. Locations hub shows all 21 cities. Sitemap lists all ~1,448 URLs organized by type.</done>
</task>

<task type="auto">
  <name>Task 2: Privacy Policy Page and Final CoreTemplate Wiring</name>
  <files>
    src/components/pages/PrivacyPolicyPage.tsx
    src/components/templates/CoreTemplate.tsx
  </files>
  <action>
    1. **PrivacyPolicyPage.tsx** (server component):
       Standard privacy policy adapted for a roofing contractor. Include these sections:

       a. H1: "Privacy Policy"
       b. Last updated date: "Last updated: March 2026"
       c. **Information We Collect**: Form submissions (name, email, phone, service interest), website usage data (cookies, analytics), device and browser information
       d. **How We Use Your Information**: Contact you about roofing services, respond to inquiries, improve our website, send service-related communications
       e. **Information Sharing**: Share with GoHighLevel CRM for lead management, do not sell personal information, may share with contractors/subcontractors for service delivery
       f. **Cookies and Tracking**: Website uses cookies for analytics and functionality, Google Analytics (placeholder), user can disable cookies in browser settings
       g. **Data Security**: Reasonable measures to protect information, SSL encryption on forms
       h. **Your Rights**: Request access to personal data, request deletion, opt out of communications. Contact us at the email/phone below.
       i. **Contact Us**: Business name, address, phone (from siteConfig), email
       j. **Changes to This Policy**: May update periodically, last updated date reflects latest changes

       Add a note at the bottom: "This privacy policy is a template and should be reviewed by a legal professional."

       Style: max-w-3xl mx-auto, prose-like typography with generous line-height, clear section headings (H2), body text in font-body

    2. **CoreTemplate.tsx** -- Complete the dispatcher:
       - Import all 4 new page components
       - Add cases for: 'services' -> ServicesHubPage, 'locations' -> LocationsHubPage, 'html-sitemap' -> SitemapPage, 'privacy-policy' -> PrivacyPolicyPage
       - Now ALL core page IDs have dedicated components (about, contact from Plan 04 + these 4)
       - The 'thank-you' case can render a simple fallback since the dedicated route handles it
       - Remove or update the default placeholder since all pages are now covered
  </action>
  <verify>
    - `npm run build` succeeds (full build validates all pages render)
    - PrivacyPolicyPage.tsx contains 'GoHighLevel', 'cookies', siteConfig import
    - CoreTemplate.tsx imports all 6 page components (AboutPage, ContactPage, ServicesHubPage, LocationsHubPage, SitemapPage, PrivacyPolicyPage)
    - Visiting /services, /locations, /sitemap, /privacy-policy all render full content (not placeholder)
    - Build output shows all 1,448+ static pages generated
  </verify>
  <done>All 8 core pages render complete content through CoreTemplate dispatcher. Privacy policy covers data collection, usage, and rights. No placeholder pages remain.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors with 1,448+ static pages
- /services shows all 63 services with residential/commercial split
- /locations shows all 21 cities with links to city pages
- /sitemap lists all URLs organized by type with working links
- /privacy-policy shows complete privacy policy text
- CoreTemplate dispatches all core page IDs to dedicated components
</verification>

<success_criteria>
- All 8 core pages accessible and rendering complete content
- Services hub has residential/commercial split with category grouping
- Locations hub links to all 21 city pages
- HTML sitemap is crawlable with links to all ~1,448 pages
- Privacy policy covers standard sections with roofing-specific adaptations
- Zero placeholder pages remain in core pages
</success_criteria>

<output>
After completion, create `.planning/phases/02-lead-capture-core-pages/02-05-SUMMARY.md`
</output>
