---
phase: 02-lead-capture-core-pages
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/thank-you/page.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/MobileMenu.tsx
autonomous: true

must_haves:
  truths:
    - "After form submission, user sees a thank-you page with next steps and timeline"
    - "Phone number is visible in the header on both desktop and mobile"
    - "Tapping the phone number on mobile initiates a call"
    - "Thank-you page has noindex meta to prevent indexing"
  artifacts:
    - path: "src/app/thank-you/page.tsx"
      provides: "Post-submission confirmation with next steps, timeline, and secondary CTA"
      contains: "noindex"
    - path: "src/components/layout/Header.tsx"
      provides: "Updated header with PhoneNumber component in nav bar"
      contains: "PhoneNumber"
  key_links:
    - from: "src/app/actions/submit-lead.ts"
      to: "src/app/thank-you/page.tsx"
      via: "redirect('/thank-you') after form submission"
      pattern: "redirect.*thank-you"
    - from: "src/components/layout/Header.tsx"
      to: "src/components/ui/PhoneNumber.tsx"
      via: "Import and render PhoneNumber in header"
      pattern: "PhoneNumber"
---

<objective>
Create the thank-you page for post-form-submission and add the click-to-call phone number to the sticky header on both desktop and mobile.

Purpose: Complete the form submission flow (submit -> redirect -> thank you) and make the phone number persistently visible on every page for click-to-call conversions.
Output: Thank-you page with next steps, header updated with phone number on all viewports.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-lead-capture-core-pages/02-CONTEXT.md
@.planning/phases/02-lead-capture-core-pages/02-RESEARCH.md

@src/components/layout/Header.tsx
@src/components/layout/MobileMenu.tsx
@src/app/layout.tsx

Read 02-01-SUMMARY.md for PhoneNumber component API.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thank-You Page</name>
  <files>
    src/app/thank-you/page.tsx
  </files>
  <action>
    Create a dedicated thank-you page at `src/app/thank-you/page.tsx` (NOT through the [slug] catch-all -- dedicated route takes precedence).

    1. Export metadata with `robots: { index: false }` to prevent indexing:
       ```
       export const metadata = {
         title: 'Thank You | Newark Quality Roofing',
         description: 'Thank you for contacting Newark Quality Roofing.',
         robots: { index: false, follow: false },
       }
       ```

    2. Page content (server component, no 'use client'):
       - Large check mark icon (SVG circle with checkmark, bg-forest text-parchment, w-16 h-16)
       - H1: "Thank You for Reaching Out!"
       - Subtitle: "We've received your request and will be in touch shortly."

    3. Next Steps section with numbered timeline:
       - Step 1: "We'll review your request" -- "Our team reviews every inquiry within the hour."
       - Step 2: "Expect a call within 24 hours" -- "A roofing specialist will call to discuss your project."
       - Step 3: "Free on-site inspection" -- "We'll schedule a convenient time to inspect your roof at no cost."

       Style as a vertical timeline with numbered circles (bg-copper, text-white, rounded-full w-8 h-8) connected by a vertical line.

    4. Emergency callout: "Need immediate help? Call us now:" with PhoneNumber component (size='lg')

    5. Secondary CTA: "While you wait, explore our services" with a link back to /services

    6. Overall layout: centered, max-w-2xl, generous padding, bg-parchment
  </action>
  <verify>
    - File exists at src/app/thank-you/page.tsx
    - Contains 'robots' with 'index: false'
    - Contains PhoneNumber import
    - `npx tsc --noEmit` passes
  </verify>
  <done>Thank-you page renders with confirmation message, 3-step timeline, emergency phone CTA, and noindex meta.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phone Number to Header and Mobile Menu</name>
  <files>
    src/components/layout/Header.tsx
    src/components/layout/MobileMenu.tsx
  </files>
  <action>
    1. **Header.tsx** -- Add phone number to the header (this is a 'use client' component, so PhoneNumber needs to be passed as a rendered element or the phone data passed as props):

       Since Header is 'use client' and PhoneNumber reads from site-config (server data), the cleanest approach is to add phone props to HeaderProps:
       - Add `phoneDisplay: string` and `phoneTel: string` to HeaderProps
       - In layout.tsx (server component), import siteConfig and pass phone.display and phone.tel to Header

       In the Header component:
       - Desktop: Add phone link BEFORE the "Get Free Estimate" CTA button, styled as a compact link with phone icon SVG + number. Use `text-parchment/80 hover:text-copper-light font-heading text-sm` styling. Wrap in an `<a href="tel:{phoneTel}">`.
       - The phone should be visible next to or before the CTA button in the right-side div
       - Add "24/7 Emergency" small text (text-xs) below or beside the phone number on desktop only

    2. **MobileMenu.tsx** -- Add phone number prominently:
       - Add `phoneDisplay: string` and `phoneTel: string` to MobileMenu props
       - At the TOP of the mobile menu (before navigation links), add a prominent click-to-call button: full-width, bg-copper, rounded-md, with phone icon + number + "Tap to Call" text
       - Style it as the most prominent element in the mobile menu

    3. **layout.tsx** -- Update to pass phone props:
       - Import siteConfig from '@/data/site-config'
       - Pass phoneDisplay={siteConfig.phone.display} phoneTel={siteConfig.phone.tel} to both Header and MobileMenu (through Header since MobileMenu is rendered inside Header)
  </action>
  <verify>
    - `npm run build` succeeds
    - Header.tsx contains 'phoneTel' and 'tel:' link
    - MobileMenu.tsx contains 'phoneTel' and 'Tap to Call'
    - layout.tsx imports siteConfig and passes phone props to Header
  </verify>
  <done>Phone number visible in header on desktop and mobile. Tapping on mobile initiates a call. Phone data flows from server (site-config) through layout to client components.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Visiting /thank-you shows confirmation page with timeline and phone number
- Header shows phone number on desktop alongside CTA
- Mobile menu shows prominent click-to-call button
- Phone links use tel: protocol
</verification>

<success_criteria>
- Thank-you page accessible at /thank-you with noindex meta
- Phone number visible in sticky header on all viewports
- Click-to-call works on mobile (tel: link)
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-lead-capture-core-pages/02-03-SUMMARY.md`
</output>
