---
phase: 07-supporting-content
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/templates/ComparisonTemplate.tsx
  - src/components/sections/ComparisonHero.tsx
  - src/components/sections/ComparisonIntro.tsx
  - src/components/sections/ComparisonTable.tsx
  - src/components/sections/ComparisonAnalysis.tsx
  - src/components/sections/ComparisonVerdict.tsx
  - src/components/sections/ComparisonFaqs.tsx
  - src/components/sections/ComparisonRelated.tsx
autonomous: true
requirements: [COMP-01, COMP-05, COMP-06, COMP-07]

must_haves:
  truths:
    - "Comparison pages render with split hero containing lead form above the fold"
    - "Side-by-side comparison table displays feature rows with item A vs item B values"
    - "Decision-helper pages render ranked list/matrix instead of A-vs-B table"
    - "Verdict section states a clear winner with alternate scenario qualifier"
    - "For Homeowners and For Business Owners subsections render within detailed analysis"
    - "FAQ accordion renders 4-6 comparison-specific questions"
    - "Related services block links to both compared service pages"
  artifacts:
    - path: "src/components/templates/ComparisonTemplate.tsx"
      provides: "Full comparison page layout with 7 section components"
      min_lines: 80
    - path: "src/components/sections/ComparisonHero.tsx"
      provides: "Split hero with lead form matching ServiceHero/ComboHero pattern"
      min_lines: 60
    - path: "src/components/sections/ComparisonTable.tsx"
      provides: "Side-by-side table or ranked matrix depending on category"
      min_lines: 40
    - path: "src/components/sections/ComparisonFaqs.tsx"
      provides: "FAQ accordion for comparison pages"
      min_lines: 25
  key_links:
    - from: "src/components/templates/ComparisonTemplate.tsx"
      to: "src/data/comparison-content/index.ts"
      via: "getComparisonContent(comparison.id)"
      pattern: "getComparisonContent"
    - from: "src/components/sections/ComparisonHero.tsx"
      to: "src/components/forms/LeadForm.tsx"
      via: "LeadForm variant='hero'"
      pattern: "LeadForm.*variant.*hero"
---

<objective>
Rebuild ComparisonTemplate from 47-line placeholder to full production layout with 7 section components. Handles all 3 comparison categories (material-vs-material, service-vs-service, decision-helper) with appropriate rendering variants.

Purpose: Provide the template infrastructure so comparison content (Plan 03) renders correctly.
Output: Full ComparisonTemplate with ComparisonHero, ComparisonIntro, ComparisonTable, ComparisonAnalysis, ComparisonVerdict, ComparisonFaqs, ComparisonRelated section components.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-supporting-content/07-CONTEXT.md
@.planning/phases/07-supporting-content/07-RESEARCH.md
@.planning/phases/07-supporting-content/07-01-SUMMARY.md

@src/components/templates/ComparisonTemplate.tsx
@src/components/sections/ComboHero.tsx
@src/components/sections/ServiceHero.tsx
@src/components/templates/ComboTemplate.tsx
@src/components/templates/ServiceTemplate.tsx
@src/components/sections/ServiceFaq.tsx
@src/components/sections/RelatedServices.tsx
@src/components/sections/Breadcrumbs.tsx
@src/components/forms/LeadForm.tsx
@src/components/sections/FloatingCtaButton.tsx
@src/components/sections/StickyFormSidebar.tsx

<interfaces>
<!-- Schemas from Plan 01 -->
From src/data/comparison-content/schema.ts:
```typescript
export const ComparisonContentSchema = z.object({
  comparisonId: z.string(),
  introHeading: z.string(),
  introParagraphs: z.array(z.string()).min(1).max(3),
  comparisonRows: z.array(z.object({
    feature: z.string(),
    itemA: z.string(),
    itemB: z.string(),
    winner: z.enum(['A', 'B', 'tie', 'depends']).optional(),
  })).min(4).max(15),
  verdict: z.object({
    winner: z.string(),
    reasoning: z.string(),
    alternateScenario: z.string(),
  }),
  detailedAnalysis: z.array(z.object({
    heading: z.string(),
    content: z.array(z.string()).min(1).max(4),
  })).min(2).max(5),
  njSpecific: z.object({ heading: z.string(), content: z.array(z.string()).min(1).max(3) }),
  homeownerSection: z.object({ heading: z.string(), content: z.array(z.string()).min(1).max(3) }),
  businessOwnerSection: z.object({ heading: z.string(), content: z.array(z.string()).min(1).max(3) }),
  faqs: z.array(z.object({ question: z.string(), answer: z.string() })).min(4).max(6),
  metaDescription: z.string().max(160),
});
export type ComparisonContent = z.infer<typeof ComparisonContentSchema>;
```

From src/lib/types.ts:
```typescript
export type Comparison = z.infer<typeof ComparisonSchema>;
// comparison.category: 'material-vs-material' | 'service-vs-service' | 'decision-helper'
// comparison.itemA?: string (absent for decision-helpers)
// comparison.itemB?: string (absent for decision-helpers)
```

From src/data/comparisons.ts:
```typescript
export const comparisons: Comparison[] = z.array(ComparisonSchema).parse(rawComparisons);
// 30 total: 16 material-vs-material, 6 service-vs-service, 8 decision-helper
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7 comparison section components</name>
  <files>
    src/components/sections/ComparisonHero.tsx
    src/components/sections/ComparisonIntro.tsx
    src/components/sections/ComparisonTable.tsx
    src/components/sections/ComparisonAnalysis.tsx
    src/components/sections/ComparisonVerdict.tsx
    src/components/sections/ComparisonFaqs.tsx
    src/components/sections/ComparisonRelated.tsx
  </files>
  <action>
    1. **ComparisonHero.tsx** — Split hero with lead form, matching ComboHero visual pattern exactly:
       - Props: { comparison: Comparison, serviceGroups: NavServiceGroup[] }
       - Dark forest-green background with gradient + grid + grain overlays (copy from ComboHero)
       - Breadcrumbs: Home > Comparisons > [Comparison Name] (use "Comparisons" since there's no hub page — just a text label, no link)
       - Left (lg:col-span-3): "Comparison" badge in copper, H1 with comparison.name, compact trust bar (Licensed NJ Contractor, Full Insurance, Free Estimates), phone CTA
       - Right (lg:col-span-2): LeadForm variant="hero" with serviceGroups
       - aria-labelledby on section pointing to H1 id

    2. **ComparisonIntro.tsx** — Introductory prose paragraphs:
       - Props: { heading: string, paragraphs: string[] }
       - H2 heading in forest, paragraphs in body font
       - Simple section with aria-labelledby

    3. **ComparisonTable.tsx** — Side-by-side comparison table or ranked matrix:
       - Props: { comparison: Comparison, rows: ComparisonContent['comparisonRows'] }
       - Conditional rendering based on comparison.category:
         * For 'material-vs-material' and 'service-vs-service': render HTML <table> with columns [Feature, itemA name, itemB name], rows from comparisonRows, highlight winner per row with subtle copper background or checkmark icon
         * For 'decision-helper': render ranked list/matrix — each row shows feature as item name, itemA as score/description, itemB as secondary info. No A-vs-B columns.
       - Table styled with Editorial Trust palette: forest header row, parchment alternating rows, copper accents for winner highlights
       - Clean and scannable — no chart libraries per user decision
       - Responsive: horizontal scroll wrapper on mobile or stack layout

    4. **ComparisonAnalysis.tsx** — Detailed analysis with audience subsections:
       - Props: { analysis: ComparisonContent['detailedAnalysis'], njSpecific: ComparisonContent['njSpecific'], homeownerSection: ComparisonContent['homeownerSection'], businessOwnerSection: ComparisonContent['businessOwnerSection'] }
       - Render each analysis item as H2 + content paragraphs
       - NJ-Specific section with H2
       - "For Homeowners" subsection with H3 heading, distinct styling (e.g., border-left accent)
       - "For Business Owners" subsection with H3 heading, distinct styling
       - Both audience sections use the user-locked dedicated subsection pattern

    5. **ComparisonVerdict.tsx** — Winner recommendation:
       - Props: { verdict: ComparisonContent['verdict'] }
       - Visually prominent section: bordered card or highlighted box with copper accent
       - H2: "Our Verdict" or "The Bottom Line"
       - Winner statement in bold
       - Reasoning paragraph
       - Alternate scenario paragraph ("However, if [scenario], [other option] is the better choice...")
       - Consultation CTA at bottom ("Not sure which is right for you? Call for a free consultation")

    6. **ComparisonFaqs.tsx** — FAQ accordion:
       - Props: { faqs: Array<{ question: string, answer: string }> }
       - Use native HTML details/summary pattern (matching ServiceFaq or FaqAccordion approach)
       - H2: "Frequently Asked Questions"
       - Each FAQ as expandable item
       - Styled consistently with existing FAQ components

    7. **ComparisonRelated.tsx** — Related services block:
       - Props: { comparison: Comparison }
       - For material-vs-material and service-vs-service: show links to both compared service pages (find services matching itemA/itemB names)
       - For decision-helper: show links to relevant service category pages
       - Use Next.js Link for internal links
       - H2: "Related Services"
       - Card-style links matching RelatedServices component pattern
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/Newarkqualityroofing && npx tsx -e "
      import './src/components/sections/ComparisonHero';
      import './src/components/sections/ComparisonIntro';
      import './src/components/sections/ComparisonTable';
      import './src/components/sections/ComparisonAnalysis';
      import './src/components/sections/ComparisonVerdict';
      import './src/components/sections/ComparisonFaqs';
      import './src/components/sections/ComparisonRelated';
      console.log('All 7 comparison section components import successfully');
    "</automated>
  </verify>
  <done>All 7 comparison section components exist with correct props interfaces, Editorial Trust styling, and responsive layouts. ComparisonTable renders differently for decision-helpers vs A-vs-B comparisons.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild ComparisonTemplate with full production layout</name>
  <files>
    src/components/templates/ComparisonTemplate.tsx
  </files>
  <action>
    Replace the 47-line placeholder ComparisonTemplate with a full production layout:

    1. Import all dependencies:
       - All 7 section components (ComparisonHero, ComparisonIntro, ComparisonTable, ComparisonAnalysis, ComparisonVerdict, ComparisonFaqs, ComparisonRelated)
       - getComparisonContent from '@/data/comparison-content' (will exist after Plan 03 creates aggregator — use try/catch for graceful fallback like ComboTemplate)
       - getServiceMenuGroups from '@/data/nav-data'
       - FloatingCtaButton, StickyFormSidebar, LeadForm
       - getMoneyPageArticle from '@/data/linking/link-engine' (for future Learn More section — Plan 10 wires it)
       - PhoneNumber from '@/components/ui/PhoneNumber'

    2. Template structure (following ComboTemplate pattern):
       ```
       <FloatingCtaButton />
       <ComparisonHero comparison={comparison} serviceGroups={serviceGroups} />
       <div className="mx-auto max-w-7xl px-6 py-12 lg:grid lg:grid-cols-3 lg:gap-12 lg:px-8">
         <div className="space-y-12 pb-16 lg:col-span-2">
           <ComparisonIntro heading={content.introHeading} paragraphs={content.introParagraphs} />
           <ComparisonTable comparison={comparison} rows={content.comparisonRows} />
           <ComparisonAnalysis ... />
           <ComparisonVerdict verdict={content.verdict} />
           <ComparisonFaqs faqs={content.faqs} />
           {/* Learn More section placeholder — wired in Plan 10 */}
         </div>
         <StickyFormSidebar>
           <LeadForm variant="standard" serviceGroups={serviceGroups} />
         </StickyFormSidebar>
       </div>
       <ComparisonRelated comparison={comparison} />
       ```

    3. Graceful fallback:
       - Try/catch around getComparisonContent() — if content doesn't exist yet, render a placeholder similar to ComboPlaceholder
       - ComparisonPlaceholder function: renders hero + placeholder message + sidebar form

    4. Props interface: { comparison: Comparison } (unchanged from current)
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/Newarkqualityroofing && npx tsx -e "
      import ComparisonTemplate from './src/components/templates/ComparisonTemplate';
      console.log('ComparisonTemplate imports successfully');
      console.log('Type:', typeof ComparisonTemplate);
    "</automated>
  </verify>
  <done>ComparisonTemplate is a full production component with 7 section components, split hero with lead form, 2-column + sidebar layout, graceful placeholder fallback, and responsive design. The 47-line placeholder is fully replaced.</done>
</task>

</tasks>

<verification>
- All 7 section components + ComparisonTemplate compile without errors
- ComparisonTemplate handles all 3 comparison categories
- ComparisonTable conditionally renders A-vs-B table or ranked matrix
- Lead form renders above the fold in hero section
- Layout matches 2-col + sidebar pattern from ServiceTemplate/ComboTemplate
</verification>

<success_criteria>
- ComparisonTemplate rebuilt from placeholder to production (>80 lines)
- Split hero with LeadForm above the fold
- Side-by-side table renders with winner highlights
- Decision-helper variant renders ranked matrix
- FAQ accordion renders expandable items
- Related services links to compared service pages
</success_criteria>

<output>
After completion, create `.planning/phases/07-supporting-content/07-02-SUMMARY.md`
</output>
