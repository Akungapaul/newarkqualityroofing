---
phase: 07-supporting-content
plan: 10
type: execute
wave: 5
depends_on: ["07-04", "07-09"]
files_modified:
  - src/app/[slug]/page.tsx
autonomous: false
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, COMP-06, COMP-07, ARTL-01, ARTL-02, ARTL-03, ARTL-04, ARTL-05]

must_haves:
  truths:
    - "Build passes with all ~1,774 pages (1,492 existing + 252 articles + 30 comparisons)"
    - "All 30 comparison pages render with full content (not placeholder)"
    - "All 252 article pages render with full content (not placeholder)"
    - "Every service page has a Learn More section linking to its first article"
    - "Article reverse silo chains are correct: article 1 -> 2 -> 3 -> back to service"
    - "No orphan article pages (all have at least money page backlink)"
    - "Comparison metadata uses hand-written metaDescription from content"
  artifacts:
    - path: "src/app/[slug]/page.tsx"
      provides: "Updated dispatcher with comparison content metadata"
  key_links:
    - from: "src/app/[slug]/page.tsx"
      to: "src/data/comparison-content/index.ts"
      via: "getComparisonContent for metadata"
      pattern: "getComparisonContent"
---

<objective>
Final integration: update comparison metadata to use hand-written metaDescription from content, run full build verification, validate reverse silo link chains, and visually verify representative pages.

Purpose: Verify the entire Phase 7 deliverable works end-to-end before marking complete.
Output: Working site with 30 comparison pages + 252 articles + reverse silo linking.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-supporting-content/07-CONTEXT.md
@.planning/phases/07-supporting-content/07-01-SUMMARY.md
@.planning/phases/07-supporting-content/07-02-SUMMARY.md
@.planning/phases/07-supporting-content/07-04-SUMMARY.md
@.planning/phases/07-supporting-content/07-09-SUMMARY.md

@src/app/[slug]/page.tsx
@src/data/comparison-content/index.ts
@src/data/article-content/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update comparison metadata and run full build verification</name>
  <files>src/app/[slug]/page.tsx</files>
  <action>
    1. Update comparison metadata in `src/app/[slug]/page.tsx`:
       - Import getComparisonContent from '@/data/comparison-content'
       - In generateMetadata switch, update 'comparison' case:
         ```
         case 'comparison': {
           const comparison = comparisons.find((c) => c.id === pageData.comparisonId);
           if (!comparison) return {};
           // Use hand-written metaDescription from comparison content when available
           let description = comparison.metaDescription;
           try {
             const content = getComparisonContent(comparison.id);
             if (content.metaDescription) {
               description = content.metaDescription;
             }
           } catch {
             // No content yet -- use base metaDescription
           }
           return {
             title: comparison.metaTitle,
             description,
           };
         }
         ```
       - Also update 'article' case metadata if needed (should already use article.metaTitle/metaDescription from Plan 01)

    2. Run full build:
       - `npx next build` must pass with zero errors
       - Expected total pages: ~1,774 (1,492 existing + 252 articles + 30 comparisons with new content)
       - Note: Page count may differ slightly since comparison pages already existed (just had placeholder content)

    3. Validate reverse silo link chains programmatically:
       - Write a quick validation script that:
         * For each service with articles: verify getMoneyPageArticle returns position-1 article
         * For each article: verify getArticleLinks returns correct moneyPage + next/prev
         * For last articles in chain: verify nextArticle is null
         * Count total articles with content via getAllArticleContent()
       - Run as npx tsx inline

    4. Run orphan audit if scripts/audit-orphan-pages.ts exists:
       - Verify no new orphan pages from the 252 articles
       - Articles should have: money page backlink (via Learn More section on parent) + forward/backward chain links
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/Newarkqualityroofing && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes with all pages. Comparison metadata uses hand-written metaDescription. Reverse silo chains validated programmatically.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of comparison pages, articles, and reverse silo links</name>
  <files>none</files>
  <action>
    Present the completed Phase 7 deliverables for human visual verification. Run `npm run dev` and guide the user through checking representative pages.
  </action>
  <what-built>
    Phase 7 complete: 30 comparison pages with full content (side-by-side tables, verdicts, FAQs) and 252 supporting articles with reverse silo linking. Service pages now show Learn More section. Homepage shows Roofing Guides section.
  </what-built>
  <how-to-verify>
    1. Visit a comparison page (e.g., /asphalt-shingles-vs-metal-roofing):
       - Verify split hero with lead form above the fold
       - Verify comparison table with side-by-side columns
       - Verify verdict section with winner recommendation
       - Verify FAQ section
       - Verify Related Reading card linking to an article

    2. Visit a decision-helper page (e.g., /best-roofing-material-nj-weather):
       - Verify ranked list/matrix instead of A-vs-B table
       - Verify verdict section

    3. Visit a service page (e.g., /roof-repair):
       - Verify new "Learn More" section appears after FAQ
       - Click through to the linked article

    4. Visit an article page (from step 3):
       - Verify full-width editorial layout (no sidebar)
       - Verify breadcrumbs: Home > [Service Name] > [Article Title]
       - Verify CTA banner at bottom linking back to service page
       - Verify "Continue Reading" card linking to next article
       - Click through to article 2, then article 3
       - Verify article 3 has no Continue Reading (only Back to Service link)

    5. Visit homepage (/):
       - Verify new "Roofing Guides" section appears after FAQ
       - Verify 3 guide cards are displayed

    6. Run the live site: `npm run dev` and check pages at http://localhost:3000
  </how-to-verify>
  <verify>User visually confirms pages render correctly</verify>
  <done>User approves visual appearance and functionality of comparison pages, articles, Learn More sections, and reverse silo navigation.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Full build passes with ~1,774 pages
- Comparison pages render full content (not placeholder)
- Article pages render full content (not placeholder)
- Reverse silo chains validated programmatically
- Visual inspection of representative pages confirms correct layout and linking
</verification>

<success_criteria>
- All 30 comparison pages render production content
- All 252 article pages render production content
- Service pages have Learn More sections
- Comparison pages have Related Reading sections
- Homepage has Roofing Guides section
- Reverse silo link chains complete and correct
- Build green, no orphan pages
- User visually approves representative pages
</success_criteria>

<output>
After completion, create `.planning/phases/07-supporting-content/07-10-SUMMARY.md`
</output>
