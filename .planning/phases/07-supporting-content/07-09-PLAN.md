---
phase: 07-supporting-content
plan: 09
type: execute
wave: 4
depends_on: ["07-05", "07-06", "07-07", "07-08"]
files_modified:
  - src/data/article-content/index.ts
  - src/components/sections/ServiceLearnMore.tsx
  - src/components/sections/ComparisonLearnMore.tsx
  - src/components/sections/HomepageGuides.tsx
  - src/components/templates/ServiceTemplate.tsx
  - src/components/templates/ComparisonTemplate.tsx
  - src/app/page.tsx
autonomous: true
requirements: [ARTL-03, ARTL-04, COMP-01]

must_haves:
  truths:
    - "Article content aggregator validates all 252 articles at import time"
    - "getArticleContent() provides O(1) lookup by articleId"
    - "Service pages show Learn More section after FAQ linking to position-1 article"
    - "Comparison pages show Learn More section after FAQ linking to position-1 article"
    - "Homepage shows Roofing Guides section linking to 3 homepage articles"
    - "Learn More sections gracefully hide when no articles exist for a service"
    - "Money page links go to position-1 article ONLY (strict POP)"
  artifacts:
    - path: "src/data/article-content/index.ts"
      provides: "Aggregator with getArticleContent() Map-based O(1) lookup"
      exports: ["getArticleContent", "getAllArticleContent"]
    - path: "src/components/sections/ServiceLearnMore.tsx"
      provides: "Learn More section for service pages"
      min_lines: 20
    - path: "src/components/sections/ComparisonLearnMore.tsx"
      provides: "Learn More section for comparison pages"
      min_lines: 20
    - path: "src/components/sections/HomepageGuides.tsx"
      provides: "Roofing Guides section for homepage"
      min_lines: 30
  key_links:
    - from: "src/data/article-content/index.ts"
      to: "src/data/article-content/schema.ts"
      via: "z.array(ArticleContentSchema).parse() at module level"
      pattern: "ArticleContentSchema.*parse"
    - from: "src/components/templates/ServiceTemplate.tsx"
      to: "src/data/linking/link-engine.ts"
      via: "getMoneyPageArticle(service.id, 'service')"
      pattern: "getMoneyPageArticle"
    - from: "src/components/templates/ComparisonTemplate.tsx"
      to: "src/data/linking/link-engine.ts"
      via: "getMoneyPageArticle(comparison.id, 'comparison')"
      pattern: "getMoneyPageArticle"
---

<objective>
Create article content aggregator, wire Learn More sections into ServiceTemplate and ComparisonTemplate, and add Roofing Guides section to homepage. This completes the reverse silo link structure: money pages link to position-1 supporting articles.

Purpose: Connect the reverse silo chain — money pages now link to their first supporting article.
Output: Article aggregator, 3 Learn More/Guides components, updated ServiceTemplate, ComparisonTemplate, and homepage.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-supporting-content/07-CONTEXT.md
@.planning/phases/07-supporting-content/07-01-SUMMARY.md
@.planning/phases/07-supporting-content/07-02-SUMMARY.md

@src/components/templates/ServiceTemplate.tsx
@src/components/templates/ComparisonTemplate.tsx
@src/app/page.tsx
@src/data/linking/link-engine.ts
@src/data/articles.ts
@src/data/article-content/schema.ts

<interfaces>
From src/data/linking/link-engine.ts (Plan 01):
```typescript
export function getMoneyPageArticle(parentId: string, parentType: string): Article | null;
```

From src/data/articles.ts (Plan 01):
```typescript
export type Article = {
  id: string;
  title: string;
  slug: string;
  parentId: string;
  parentType: 'service' | 'comparison' | 'core';
  position: number;
  metaTitle: string;
  metaDescription: string;
};
```

Article content files from Plans 05-08:
- repair-maintenance.ts, residential-roof-types.ts, commercial-roof-types.ts
- components-specialty.ts, replacement-sub-pages.ts, energy-solar.ts
- commercial-services.ts, design-consultation.ts, comparisons.ts, homepage.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create article content aggregator and Learn More components</name>
  <files>
    src/data/article-content/index.ts
    src/components/sections/ServiceLearnMore.tsx
    src/components/sections/ComparisonLearnMore.tsx
    src/components/sections/HomepageGuides.tsx
  </files>
  <action>
    1. Create `src/data/article-content/index.ts` (aggregator):
       - Import z from 'zod'
       - Import ArticleContentSchema and ArticleContent from './schema'
       - Import all 10 content arrays from content files:
         * repairMaintenanceArticles, residentialRoofTypesArticles, commercialRoofTypesArticles
         * componentsSpecialtyArticles, replacementSubPagesArticles, energySolarArticles
         * commercialServicesArticles, designConsultationArticles, comparisonArticles, homepageArticles
       - Collect all: const allContent = [...all arrays]
       - Validate at module level: z.array(ArticleContentSchema).parse(allContent)
       - Build Map<string, ArticleContent> keyed by articleId
       - Export getArticleContent(articleId: string): ArticleContent (throws on missing)
       - Export getAllArticleContent(): ArticleContent[]
       - Follow exact pattern from src/data/combo-content/index.ts

    2. Create `src/components/sections/ServiceLearnMore.tsx`:
       - Props: { article: Article, serviceName: string }
       - Renders a "Learn More" section with:
         * H2: "Learn More About [serviceName]"
         * Styled card (copper accent border, parchment background) linking to article.slug
         * Article title as card heading
         * "Continue reading..." subtext
       - Uses Next.js Link
       - Placement: after FAQ section, before CTA banner in ServiceTemplate

    3. Create `src/components/sections/ComparisonLearnMore.tsx`:
       - Props: { article: Article, comparisonName: string }
       - Same pattern as ServiceLearnMore but for comparison context
       - H2: "Related Reading"
       - Card linking to article.slug

    4. Create `src/components/sections/HomepageGuides.tsx`:
       - Props: { articles: Article[] } (expects 3 articles)
       - H2: "Roofing Guides for NJ Homeowners"
       - 3 editorial cards in a grid (lg:grid-cols-3)
       - Each card: article title, brief description from metaDescription, "Read Guide" link
       - Styled with Editorial Trust palette
       - Placement: after FAQ section on homepage, before final CTA
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/Newarkqualityroofing && npx tsx -e "
      import { getArticleContent, getAllArticleContent } from './src/data/article-content';
      const all = getAllArticleContent();
      console.log('Total article content:', all.length, '(expect 252)');
      const test = getArticleContent(all[0].articleId);
      console.log('First article:', test.articleId, '- intro length:', test.intro.length);
    "</automated>
  </verify>
  <done>Article content aggregator validates all 252 articles at import time. ServiceLearnMore, ComparisonLearnMore, and HomepageGuides components exist with correct styling and link behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Learn More sections into ServiceTemplate, ComparisonTemplate, and homepage</name>
  <files>
    src/components/templates/ServiceTemplate.tsx
    src/components/templates/ComparisonTemplate.tsx
    src/app/page.tsx
  </files>
  <action>
    1. Update `src/components/templates/ServiceTemplate.tsx`:
       - Import getMoneyPageArticle from '@/data/linking/link-engine'
       - Import ServiceLearnMore from '@/components/sections/ServiceLearnMore'
       - In the template body, after ServiceFaq and before the closing </div> of the main content column:
         ```
         {/* Learn More — reverse silo link to position-1 article */}
         {(() => {
           const article = getMoneyPageArticle(service.id, 'service');
           return article ? <ServiceLearnMore article={article} serviceName={service.name} /> : null;
         })()}
         ```
       - This gracefully renders nothing if no article exists for this service
       - Placement: AFTER ServiceFaq, BEFORE closing the lg:col-span-2 div (inside the main content column)
       - This ensures the Learn More section is the LAST content section before the sidebar ends

    2. Update `src/components/templates/ComparisonTemplate.tsx`:
       - Import getMoneyPageArticle from '@/data/linking/link-engine'
       - Import ComparisonLearnMore from '@/components/sections/ComparisonLearnMore'
       - In the template body, after ComparisonFaqs:
         ```
         {(() => {
           const article = getMoneyPageArticle(comparison.id, 'comparison');
           return article ? <ComparisonLearnMore article={article} comparisonName={comparison.name} /> : null;
         })()}
         ```
       - Same graceful degradation pattern

    3. Update `src/app/page.tsx` (homepage):
       - Import HomepageGuides from '@/components/sections/HomepageGuides'
       - Import { articles } from '@/data/articles'
       - Filter homepage articles: articles.filter(a => a.parentType === 'core' && a.parentId === 'homepage')
       - Sort by position
       - Add <HomepageGuides articles={homepageArticles} /> after FaqAccordion and before the final CTA section
       - Graceful: only render if articles array is non-empty

    IMPORTANT: These are MINIMAL changes to existing templates. Only add the Learn More/Guides section. Do not restructure existing template layout.
    POP RULE: Money page links to ONE article only (position 1). The Learn More section renders a single card, not multiple.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/Newarkqualityroofing && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>ServiceTemplate shows Learn More section linking to position-1 article after FAQ. ComparisonTemplate shows Related Reading linking to position-1 article. Homepage shows Roofing Guides section with 3 editorial cards. All gracefully hide when no articles exist. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds with all pages rendering
- Service pages display Learn More card after FAQ section
- Comparison pages display Related Reading card after FAQ section
- Homepage displays 3 Roofing Guides cards
- Learn More sections link to position-1 article only (strict POP)
- Missing articles cause graceful hiding (not errors)
</verification>

<success_criteria>
- Article content aggregator validates 252 articles at import time
- Service pages link to their first supporting article
- Comparison pages link to their first supporting article
- Homepage displays Roofing Guides section
- Reverse silo chain complete: money page -> article 1 -> article 2 -> article 3 -> back to money page
- Build passes with all ~1,774 pages
</success_criteria>

<output>
After completion, create `.planning/phases/07-supporting-content/07-09-SUMMARY.md`
</output>
