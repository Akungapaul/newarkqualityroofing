---
phase: 10-add-image-creation-system
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/sections/BeforeAfterGallery.tsx
  - src/components/sections/HomeResidentialCommercial.tsx
  - src/components/sections/HomeWhyChooseUs.tsx
  - src/components/sections/HomePricingTable.tsx
  - src/components/sections/HomeRepairServices.tsx
  - src/components/sections/FaqAccordion.tsx
  - src/components/sections/LocationsGrid.tsx
  - src/app/page.tsx
  - src/app/[slug]/page.tsx
  - src/lib/seo-config.ts
autonomous: true
requirements:
  - IMG-11
  - IMG-12
  - IMG-13

must_haves:
  truths:
    - "All 8 components with hardcoded image paths now use manifest lookup helpers instead"
    - "Components gracefully fall back to existing stock photos when manifest has no entry (images not yet generated)"
    - "buildOG() accepts optional per-page OG image path, falling back to SEO_CONFIG.OG_IMAGE"
    - "Dead badge files (bbb, gaf, homeadvisor) are removed from public/images/"
    - "The site builds successfully with next build after all changes"
  artifacts:
    - path: "src/components/sections/BeforeAfterGallery.tsx"
      provides: "Gallery using manifest getGalleryPairs() with fallback to current hardcoded paths"
      min_lines: 30
    - path: "src/app/[slug]/page.tsx"
      provides: "buildOG() with optional ogImagePath parameter for per-page OG images"
      min_lines: 40
  key_links:
    - from: "src/components/sections/BeforeAfterGallery.tsx"
      to: "src/data/image-manifest.ts"
      via: "getGalleryPairs() lookup"
      pattern: "import.*getGalleryPairs.*from.*image-manifest"
    - from: "src/components/sections/HomeWhyChooseUs.tsx"
      to: "src/data/image-manifest.ts"
      via: "getHomepageImages() lookup"
      pattern: "import.*getHomepageImages.*from.*image-manifest"
    - from: "src/app/[slug]/page.tsx"
      to: "src/data/image-manifest.ts"
      via: "getOGImage() for per-page OG images"
      pattern: "import.*getOGImage.*from.*image-manifest"
---

<objective>
Integrate the image manifest into all site components that currently use hardcoded image paths, update the OG metadata system for per-page OG images, and clean up dead files. All changes include graceful fallbacks so the site works both before and after images are generated.

Purpose: This is the bridge between the CLI toolset (Plans 01-03) and the live site. After this plan, generating and approving images via the CLI automatically updates what the site displays.
Output: All 8 image-referencing components use manifest lookups, buildOG() supports per-page OG images, dead files removed, site builds cleanly.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-add-image-creation-system/10-CONTEXT.md
@.planning/phases/10-add-image-creation-system/10-RESEARCH.md
@.planning/phases/10-add-image-creation-system/10-01-SUMMARY.md
@src/components/sections/BeforeAfterGallery.tsx
@src/components/sections/HomeResidentialCommercial.tsx
@src/components/sections/HomeWhyChooseUs.tsx
@src/components/sections/HomePricingTable.tsx
@src/components/sections/HomeRepairServices.tsx
@src/components/sections/FaqAccordion.tsx
@src/components/sections/LocationsGrid.tsx
@src/app/page.tsx
@src/app/[slug]/page.tsx
@src/lib/seo-config.ts

<interfaces>
<!-- Key exports from Plan 01 manifest -->

From src/data/image-manifest.ts:
- imageManifest: ImageEntry[]
- getServiceHeroImage(serviceId: string): ImageEntry | undefined
- getCityHeroImage(cityId: string): ImageEntry | undefined
- getOGImage(pageType: string, pageId: string): ImageEntry | undefined
- getHomepageImages(): ImageEntry[]
- getGalleryPairs(): Array<{ before: ImageEntry, after: ImageEntry }>
- getContentPoolImages(subcategory: string): ImageEntry[]

ImageEntry: {
  id: string, filename: string, path: string, alt: string,
  width: number, height: number, category: string,
  pageType?: string, pageId?: string, prompt: string, generatedAt?: string
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update homepage components to use manifest lookups</name>
  <files>
    src/components/sections/BeforeAfterGallery.tsx
    src/components/sections/HomeResidentialCommercial.tsx
    src/components/sections/HomeWhyChooseUs.tsx
    src/components/sections/HomePricingTable.tsx
    src/components/sections/HomeRepairServices.tsx
    src/components/sections/FaqAccordion.tsx
    src/components/sections/LocationsGrid.tsx
    src/app/page.tsx
  </files>
  <action>
Update all 8 components that currently hardcode image paths from `/images/*.jpg` to use manifest lookup helpers. Every component MUST include a graceful fallback to the existing stock photo path when the manifest entry doesn't exist (manifest is empty until images are generated and approved).

**Pattern for each component:**
```tsx
import { getHomepageImages } from '@/data/image-manifest';

// Lookup with fallback
const heroImage = getHomepageImages().find(i => i.id === 'hero-main');
const heroSrc = heroImage?.path ? `/${heroImage.path}` : '/images/free-roofing-consultation.jpg';
const heroAlt = heroImage?.alt ?? 'Free roofing consultation in Newark NJ';
```

Note: manifest `path` is stored relative to public/ (e.g., "images/homepage/hero-main.webp") so prepend "/" for the src attribute.

**Component-specific updates:**

1. **BeforeAfterGallery.tsx**: Replace 4 hardcoded before/after pairs with `getGalleryPairs()` call. If manifest returns fewer than the minimum needed, fall back to current hardcoded array. Expand the projects array to support 6-8 pairs when available (current: 4 pairs). Keep the existing clipPath slider interaction unchanged.

2. **HomeResidentialCommercial.tsx**: Replace 2 hardcoded paths (residential-roof-repair-newark.jpg, commercial-roofing-newark.jpg) with manifest lookups for ids 'residential-split' and 'commercial-split'.

3. **HomeWhyChooseUs.tsx**: Replace 1 hardcoded path (newark-roofing-team.jpg) with manifest lookup for id 'why-team'. (Note: only the large image uses a photo; the 4 stat cards use icons/text.)

4. **HomePricingTable.tsx**: Replace 1 hardcoded path (affordable-roofing-estimate.jpg) with manifest lookup for id 'pricing-estimate'.

5. **HomeRepairServices.tsx**: Replace 1 hardcoded path (roof-replacement-newark.jpg) with manifest lookup for id 'repair-services'.

6. **FaqAccordion.tsx**: Replace 1 hardcoded path (homeowner-roofing-options.jpg) with manifest lookup for id 'faq-options'.

7. **LocationsGrid.tsx**: Replace 1 hardcoded path (essex-county-service-map.jpg) with manifest lookup for id 'service-map'.

8. **page.tsx (homepage)**: Replace 1 hardcoded path (free-roofing-consultation.jpg) with manifest lookup for id 'consultation'.

**IMPORTANT**: These are all server components (except BeforeAfterGallery which is a client component). For client components, the manifest lookup must happen server-side and be passed as props, OR the manifest can be imported directly since it's a static data file with no server-only dependencies. Since image-manifest.ts is a pure data file (Zod + arrays), it can be imported in client components safely.

**alt text**: When manifest entry exists, use its `alt` field. When falling back, keep the existing alt text.

**Image dimensions**: Keep existing width/height attributes on Next.js Image components. The manifest has dimensions for reference but the component's layout dimensions are what matters.

After updates, remove the 3 dead badge files:
- `public/images/bbb-accredited-badge.jpg` (0 bytes)
- `public/images/gaf-certified-badge.jpg` (0 bytes)
- `public/images/homeadvisor-badge.jpg` (0 bytes)

Verify none of these are referenced anywhere with a grep before deleting.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/Newarkqualityroofing && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>All 8 components use manifest lookups with graceful fallbacks to existing stock photos. Dead badge files removed. The site builds successfully with `next build`. When the manifest is empty, all components render exactly as before (using fallback stock photos). When manifest entries exist, components use the AI-generated images.</done>
</task>

<task type="auto">
  <name>Task 2: Per-page OG images in metadata system</name>
  <files>
    src/app/[slug]/page.tsx
    src/lib/seo-config.ts
  </files>
  <action>
Update the metadata system to support per-page OG images while maintaining the shared fallback.

**src/app/[slug]/page.tsx** updates:

1. Import `getOGImage` from `@/data/image-manifest`

2. Update `buildOG()` function signature to accept an optional `ogImagePath` parameter:
```typescript
function buildOG(
  title: string,
  description: string,
  slug: string,
  type: 'website' | 'article' = 'website',
  ogImagePath?: string
) {
```

3. In the `images` array within buildOG(), use the custom OG image if provided, otherwise fall back to SEO_CONFIG.OG_IMAGE:
```typescript
images: [
  ogImagePath
    ? { url: ogImagePath, width: 1200, height: 630 }
    : { url: SEO_CONFIG.OG_IMAGE.url, width: SEO_CONFIG.OG_IMAGE.width, height: SEO_CONFIG.OG_IMAGE.height },
],
```

4. Update each `generateMetadata` case to look up the per-page OG image:

- **Service pages**: `const ogImage = getOGImage('service', service.id); buildOG(..., ogImage?.path ? '/' + ogImage.path : undefined)`
- **City pages**: `const ogImage = getOGImage('city', city.id); buildOG(..., ogImage?.path ? '/' + ogImage.path : undefined)`
- **Combo pages**: Reuse service OG image: `const ogImage = getOGImage('service', combo.serviceId); buildOG(..., ogImage?.path ? '/' + ogImage.path : undefined)`
- **Comparison pages**: Use default (no custom OG this phase -- deferred per CONTEXT.md)
- **Article pages**: Use default (no custom OG this phase -- deferred per CONTEXT.md)
- **Core pages**: For homepage slug check, use `getOGImage('homepage', 'homepage')`. Others use default.

5. **src/app/page.tsx** (homepage metadata): Update the homepage's metadata export to use per-page OG image if available. Import `getOGImage` and look up `getOGImage('homepage', 'homepage')`. If found, use its path; otherwise keep current SEO_CONFIG.OG_IMAGE fallback.

**src/lib/seo-config.ts** -- No changes needed. SEO_CONFIG.OG_IMAGE remains as the fallback. Its value stays as `/images/og-default.jpg` (or whatever exists).

**Key requirement**: All metadata changes must produce the same output as before when the manifest is empty (graceful degradation). The per-page OG images only take effect after images are generated, approved, and OG composites created.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/Newarkqualityroofing && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>buildOG() accepts optional per-page OG image path. Service, city, combo, and homepage metadata look up per-page OG images from manifest. When manifest is empty, all pages use the shared SEO_CONFIG.OG_IMAGE fallback (zero visual change). When OG images exist in manifest, each page gets its own social sharing image. Site builds successfully.</done>
</task>

</tasks>

<verification>
- `next build` completes with zero errors
- `grep -r "src=\"/images/" src/components/sections/ src/app/page.tsx | grep -v manifest | grep -v fallback` shows no remaining hardcoded image paths (all replaced with manifest lookups + fallbacks)
- `ls public/images/bbb-accredited-badge.jpg public/images/gaf-certified-badge.jpg public/images/homeadvisor-badge.jpg 2>&1` shows "No such file" for all 3 dead files
- Homepage renders correctly in dev mode (images fall back to stock photos)
</verification>

<success_criteria>
- All 8 components use manifest lookups with graceful fallback to existing stock photos
- BeforeAfterGallery supports 6-8 pairs (expandable) while falling back to current 4 pairs
- buildOG() accepts optional ogImagePath parameter for per-page OG images
- Service, city, combo, and homepage pages look up per-page OG images from manifest
- 3 dead badge files removed from public/images/
- next build succeeds with zero errors
- Site displays identically to before when manifest is empty (all fallbacks work)
</success_criteria>

<output>
After completion, create `.planning/phases/10-add-image-creation-system/10-04-SUMMARY.md`
</output>
