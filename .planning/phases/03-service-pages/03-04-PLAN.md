---
phase: 03-service-pages
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/data/service-content/replacement-sub-pages.ts
  - src/data/service-content/index.ts
autonomous: true

must_haves:
  truths:
    - "All 15 replacement sub-page services have 2,000+ words of content each"
    - "All 63 services are accessible via getServiceContent(serviceId) without errors"
    - "Zod validation passes for all 63 ServiceContent objects at module load time"
    - "Service pages render real content instead of placeholder text"
    - "next build succeeds with all 63 service pages fully populated"
  artifacts:
    - path: "src/data/service-content/replacement-sub-pages.ts"
      provides: "Content for 15 replacement sub-pages"
      contains: "replacementSubPagesContent"
    - path: "src/data/service-content/index.ts"
      provides: "Content aggregation, Zod validation, lookup function"
      exports: ["getServiceContent"]
  key_links:
    - from: "src/data/service-content/index.ts"
      to: "src/lib/schemas.ts"
      via: "Zod validation with ServiceContentSchema"
      pattern: "z\\.array.*ServiceContentSchema.*parse"
    - from: "src/data/service-content/index.ts"
      to: "src/components/templates/ServiceTemplate.tsx"
      via: "getServiceContent called from template"
      pattern: "getServiceContent"
    - from: "src/data/service-content/index.ts"
      to: "src/data/service-content/repair-maintenance.ts"
      via: "import and aggregate"
      pattern: "repairMaintenanceContent"
---

<objective>
Complete the content layer: 15 replacement sub-pages, content index with Zod validation and lookup function, full integration so all 63 service pages render real content.

Purpose: Finalizes Phase 3 by creating the last content batch (replacement sub-pages), wiring all 8 content files through a validated index with Map-based lookup, and replacing the placeholder content fallback in ServiceTemplate with real content for all 63 services.

Output: replacement-sub-pages.ts content file, index.ts aggregator with getServiceContent(), all 63 service pages rendering full content.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-service-pages/03-CONTEXT.md
@.planning/phases/03-service-pages/03-RESEARCH.md
@src/data/services.ts
@src/lib/schemas.ts
@src/lib/types.ts
@src/components/templates/ServiceTemplate.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replacement sub-pages content (15 services)</name>
  <files>src/data/service-content/replacement-sub-pages.ts</files>
  <action>
    Create src/data/service-content/replacement-sub-pages.ts exporting replacementSubPagesContent: ServiceContent[].

    15 services (match IDs from services.ts -- note: the project originally had 17 but trimmed to 15 per decision [01-02]):
    1. full-roof-tear-off
    2. roof-overlay-installation
    3. re-roofing
    4. insurance-roof-replacement
    5. storm-damage-roof-replacement
    6. aging-roof-replacement
    7. roof-replacement-after-leak
    8. fire-damage-roof-replacement
    9. roof-replacement-cost
    10. asphalt-shingle-roof-replacement
    11. metal-roof-replacement
    12. slate-roof-replacement
    13. tile-roof-replacement
    14. flat-roof-replacement
    15. cedar-shake-roof-replacement

    IMPORTANT: Verify these 15 IDs against src/data/services.ts. The services.ts file is the source of truth for service IDs. If any ID doesn't match, use the ID from services.ts.

    Content guidance for replacement sub-pages:
    - These are HIGH-VALUE lead pages (roof replacement is the most expensive service). Content should be thorough and conversion-focused.
    - Overview: Why replacement (not just repair), when replacement is the better financial decision, cost-benefit framing.
    - Signs: Indicators that replacement is needed vs repair (age of roof, extent of damage, multiple prior repairs, insurance adjuster recommendation).
    - Approach: Full replacement process, materials available, timeline expectations.
    - For material-specific replacements (asphalt, metal, slate, tile, flat, cedar): focus on that material's replacement specifics (removal of old material, substrate prep, new installation).
    - For cause-specific replacements (insurance, storm, aging, leak, fire): focus on the cause-specific process (insurance claims, emergency tarping, documentation, code upgrades).
    - "Roof Replacement Cost" page: uniquely focused on pricing transparency, factors affecting cost, financing options, insurance coverage, average NJ costs.
    - Residential: Investment in home value, financing options, choosing the right material, timeline living under construction.
    - Commercial: Asset protection, phased replacement to minimize business disruption, warranty packages, code upgrades.

    Import type: `import type { ServiceContent } from '@/lib/types';`
  </action>
  <verify>
    Run `npx tsc --noEmit` -- zero type errors.
    Verify all 15 serviceIds present.
    Word count: 30,000+ words (15 x 2,000+).
  </verify>
  <done>15 replacement sub-page services have complete, conversion-focused content with 2,000+ words each, covering both cause-specific and material-specific replacement scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: Content index with Zod validation and full integration</name>
  <files>
    src/data/service-content/index.ts
  </files>
  <action>
    Replace the stub src/data/service-content/index.ts (created in Plan 01) with the full aggregation module:

    1. Import all 8 content arrays:
       - repairMaintenanceContent from ./repair-maintenance
       - residentialRoofTypesContent from ./residential-roof-types
       - commercialRoofTypesContent from ./commercial-roof-types
       - componentsSpecialtyContent from ./components-specialty
       - energySolarContent from ./energy-solar
       - commercialServicesContent from ./commercial-services
       - designConsultationContent from ./design-consultation
       - replacementSubPagesContent from ./replacement-sub-pages

    2. Import { z } from 'zod' and { ServiceContentSchema } from '@/lib/schemas'

    3. Re-export ServiceContent type: `export type { ServiceContent } from '@/lib/types';`

    4. Aggregate and validate ALL content at module level:
       ```
       const allContent = z.array(ServiceContentSchema).parse([
         ...repairMaintenanceContent,
         ...residentialRoofTypesContent,
         ...commercialRoofTypesContent,
         ...componentsSpecialtyContent,
         ...energySolarContent,
         ...commercialServicesContent,
         ...designConsultationContent,
         ...replacementSubPagesContent,
       ]);
       ```
       This crashes the build if ANY content object is invalid -- consistent with project pattern (services.ts, cities.ts use same approach).

    5. Build Map for O(1) lookup:
       ```
       const contentMap = new Map(allContent.map(c => [c.serviceId, c]));
       ```

    6. Export getServiceContent function:
       ```
       export function getServiceContent(serviceId: string): ServiceContent {
         const content = contentMap.get(serviceId);
         if (!content) throw new Error(`Missing content for service: ${serviceId}`);
         return content;
       }
       ```

    7. Verify the ServiceTemplate.tsx no longer needs placeholder fallback. The try/catch in ServiceTemplate (from Plan 01) should now successfully call getServiceContent() for all 63 services. If the template still has a fallback/placeholder path, consider removing it or leaving it as a safety net -- either is fine as long as real content renders.

    8. Run `npx next build` to verify ALL 63 service pages build successfully with real content. The build will crash if:
       - Any serviceId in content files doesn't match a valid service
       - Any content object fails Zod validation
       - Any service is missing content (getServiceContent throws)
  </action>
  <verify>
    Run `npx tsc --noEmit` -- zero type errors.
    Run `npx next build 2>&1 | tail -30` -- build succeeds, all static pages generated.
    Verify content count: `grep -c "serviceId:" src/data/service-content/index.ts` or count the spread arrays -- should aggregate to 63.
    Open dev server and spot-check 3 service pages (one from each wave): /roof-repair, /asphalt-shingle-roofing, /commercial-roof-installation -- verify real content renders (not placeholder).
  </verify>
  <done>All 63 services have validated content accessible via getServiceContent(). Zod validation passes at module load. next build succeeds with all service pages rendering full content. No placeholder text remains on any service page.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx next build` completes with all ~1,448+ static pages (63 service pages now with real content)
- getServiceContent() returns valid content for all 63 service IDs
- Zod validation at module load catches any schema violations
- Spot-check service pages show: breadcrumbs, split hero with form, all 10 content sections with real text, sticky sidebar, related services, bottom CTA
- Commercial-first services render commercial section before residential
- Word count across all content files exceeds 126,000 words (63 x 2,000+)
</verification>

<success_criteria>
1. 15 replacement sub-page content objects created and validated
2. index.ts aggregates all 63 ServiceContent objects with Zod validation
3. getServiceContent() works for every service ID without errors
4. next build succeeds -- all 63 service pages render with real content
5. No placeholder content visible on any service page
6. Phase 3 success criteria met: 63 pages, 2,000+ words, H1/H2/H3 hierarchy, residential+commercial, forms above fold, related services, breadcrumbs
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-pages/03-04-SUMMARY.md`
</output>
