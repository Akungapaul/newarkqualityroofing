---
phase: 03-service-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/schemas.ts
  - src/lib/types.ts
  - src/components/sections/Breadcrumbs.tsx
  - src/components/sections/ServiceHero.tsx
  - src/components/sections/ServiceOverview.tsx
  - src/components/sections/ServiceSigns.tsx
  - src/components/sections/ServiceApproach.tsx
  - src/components/sections/ServiceAudience.tsx
  - src/components/sections/ServiceProcess.tsx
  - src/components/sections/ServiceFaq.tsx
  - src/components/sections/RelatedServices.tsx
  - src/components/sections/ServiceCtaBanner.tsx
  - src/components/sections/StickyFormSidebar.tsx
  - src/components/sections/FloatingCtaButton.tsx
  - src/components/templates/ServiceTemplate.tsx
autonomous: true

must_haves:
  truths:
    - "Service pages have a split hero with H1 + bullets left and LeadForm right"
    - "Breadcrumbs show Home > Services > [Service Name] above the hero"
    - "Content area has ~65% width with sticky sidebar form on desktop"
    - "Mobile shows floating CTA button at viewport bottom"
    - "Commercial-first ordering is supported for 5 dedicated commercial services"
    - "10 content sections render in problem-solution-process order"
  artifacts:
    - path: "src/lib/schemas.ts"
      provides: "ServiceContentSchema"
      contains: "ServiceContentSchema"
    - path: "src/components/sections/Breadcrumbs.tsx"
      provides: "Reusable breadcrumb navigation"
      contains: "aria-label"
    - path: "src/components/sections/ServiceHero.tsx"
      provides: "Split hero with form"
      contains: "LeadForm"
    - path: "src/components/sections/FloatingCtaButton.tsx"
      provides: "Mobile floating CTA"
      contains: "use client"
    - path: "src/components/templates/ServiceTemplate.tsx"
      provides: "Full service page template"
      min_lines: 60
  key_links:
    - from: "src/components/templates/ServiceTemplate.tsx"
      to: "src/components/sections/ServiceHero.tsx"
      via: "import and render"
      pattern: "ServiceHero"
    - from: "src/components/sections/ServiceHero.tsx"
      to: "src/components/forms/LeadForm.tsx"
      via: "import and render with defaultService"
      pattern: "LeadForm.*defaultService"
    - from: "src/components/templates/ServiceTemplate.tsx"
      to: "src/data/nav-data.ts"
      via: "getServiceMenuGroups call on server"
      pattern: "getServiceMenuGroups"
---

<objective>
Build the ServiceTemplate component and all section components for service pages.

Purpose: Creates the complete service page layout with 10 content sections, split hero with lead form, sticky sidebar, breadcrumbs, related services, and mobile floating CTA. This is the template infrastructure that content plans (02, 03, 04) will populate.

Output: Rebuilt ServiceTemplate.tsx with all section components, ServiceContentSchema in schemas.ts, FloatingCtaButton client component.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-service-pages/03-CONTEXT.md
@.planning/phases/03-service-pages/03-RESEARCH.md
@src/components/templates/ServiceTemplate.tsx
@src/components/sections/HeroSection.tsx
@src/components/forms/LeadForm.tsx
@src/components/sections/FaqAccordion.tsx
@src/lib/schemas.ts
@src/lib/types.ts
@src/data/services.ts
@src/data/nav-data.ts
@src/app/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ServiceContentSchema and section components</name>
  <files>
    src/lib/schemas.ts
    src/lib/types.ts
    src/components/sections/Breadcrumbs.tsx
    src/components/sections/ServiceHero.tsx
    src/components/sections/ServiceOverview.tsx
    src/components/sections/ServiceSigns.tsx
    src/components/sections/ServiceApproach.tsx
    src/components/sections/ServiceAudience.tsx
    src/components/sections/ServiceProcess.tsx
    src/components/sections/ServiceFaq.tsx
    src/components/sections/RelatedServices.tsx
    src/components/sections/ServiceCtaBanner.tsx
    src/components/sections/StickyFormSidebar.tsx
    src/components/sections/FloatingCtaButton.tsx
  </files>
  <action>
    1. Add ServiceContentSchema to src/lib/schemas.ts (do NOT modify existing schemas):
       - serviceId: z.string()
       - overview: z.array(z.string()).min(2).max(3) -- 2-3 paragraphs
       - signsHeading: z.string() -- e.g. "Signs You Need Roof Repair"
       - signs: z.array(z.string()).min(4).max(8)
       - approachHeading: z.string()
       - approachContent: z.array(z.string()).min(2).max(4)
       - residential: z.object({ heading, content: z.array(z.string()).min(2).max(4), ctaLabel })
       - commercial: z.object({ heading, content: z.array(z.string()).min(2).max(4), ctaLabel })
       - processSteps: z.array(z.object({ title, description })).min(4).max(7)
       - faqs: z.array(z.object({ question, answer })).min(4).max(6)

    2. Add ServiceContent type export to src/lib/types.ts.

    3. Create section components (all server components except FloatingCtaButton):

       **Breadcrumbs.tsx**: Semantic `<nav aria-label="Breadcrumb">` with ordered list. Props: items: { label: string; href?: string }[]. Last item has no href (aria-current="page"). Use parchment/copper colors on dark backgrounds. Separator: "/" character with aria-hidden.

       **ServiceHero.tsx**: Split layout -- lg:grid-cols-5 (3 left, 2 right). Left side: H1 with service.name, 3 scannable benefit bullets (derive from shortDescription or pass as props), PhoneNumber component. Right side: LeadForm with variant="hero", defaultService={service.slug}. Props: service (Service type), serviceGroups (NavServiceGroup[]), benefits (string[]). Background: forest-dark with layered gradient treatment (follow HeroSection.tsx pattern). Breadcrumbs rendered above the grid content spanning full width. Add id="lead-form" to the form wrapper div for mobile anchor linking.

       **ServiceOverview.tsx**: 2-3 paragraphs under an H2. Props: paragraphs: string[]. Each paragraph in its own `<p>` tag with proper font-body styling.

       **ServiceSigns.tsx**: H2 heading + bullet list of signs/symptoms. Props: heading: string, signs: string[]. Use checkmark or warning icons. Scannable layout with adequate spacing.

       **ServiceApproach.tsx**: H2 heading + 2-4 paragraphs about methodology. Props: heading: string, content: string[].

       **ServiceAudience.tsx**: Reusable for both residential and commercial. Props: variant ("residential" | "commercial"), heading: string, content: string[], ctaLabel: string. Residential uses forest/green accent, commercial uses copper accent. Each has its own CTA button (anchor to #lead-form). H2 heading.

       **ServiceProcess.tsx**: Numbered steps (4-7). Props: steps: { title: string; description: string }[]. Visual numbered list with step numbers as decorative circles. H2 "Our Process".

       **ServiceFaq.tsx**: Wraps the FAQ data into details/summary accordion pattern (follow FaqAccordion.tsx pattern but accept service-specific FAQ data). Props: faqs: { question: string; answer: string }[]. H2 "Frequently Asked Questions". Do NOT reuse FaqAccordion directly (it has its own section wrapper/heading) -- create a standalone accordion that fits within the content column.

       **RelatedServices.tsx**: 3-4 cards from same category. Props: services: { name: string; slug: string; shortDescription: string }[]. Card with service name, brief description, "Learn More" link. H2 "Related Services". Use Link from next/link.

       **ServiceCtaBanner.tsx**: Full-width CTA section at bottom with inline LeadForm. Props: serviceGroups (NavServiceGroup[]), defaultService (string). H2 "Ready to Get Started?". Dark background (forest-dark), LeadForm with variant="standard".

       **StickyFormSidebar.tsx**: Wrapper with `sticky top-24` positioning. Props: children (ReactNode). Hidden on mobile (hidden lg:block). Rendered in the aside column.

       **FloatingCtaButton.tsx**: Client component ('use client'). Fixed bottom bar on mobile only (lg:hidden). Links to #lead-form anchor. "Get Free Estimate" text. z-40, forest-dark background, copper button. aria-label for accessibility.
  </action>
  <verify>
    Run `npx tsc --noEmit` -- zero type errors.
    Verify ServiceContentSchema exists: `grep "ServiceContentSchema" src/lib/schemas.ts`
    Verify FloatingCtaButton is client: `grep "use client" src/components/sections/FloatingCtaButton.tsx`
  </verify>
  <done>All 12 section components created with correct props, ServiceContentSchema added to schemas.ts, ServiceContent type exported from types.ts, TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild ServiceTemplate with full layout</name>
  <files>
    src/components/templates/ServiceTemplate.tsx
  </files>
  <action>
    Completely rewrite ServiceTemplate.tsx to compose all section components into the 10-section layout from CONTEXT.md.

    ServiceTemplate is a SERVER component. It receives a Service object from the [slug] page dispatcher. It must:

    1. Import getServiceMenuGroups from nav-data.ts and call it (server-side, no client bundle impact)
    2. Import getServiceContent from the content data module (will be created in plans 02-04; for now, use a try/catch that falls back to placeholder content if getServiceContent throws -- this lets the template work before content is populated)
    3. Import services from data/services.ts for related services lookup
    4. Determine if this is a commercial-first service (check service.id against: commercial-roof-installation, commercial-roof-repair, commercial-roof-replacement, roof-thermal-imaging-inspections, infrared-roof-leak-detection)
    5. Get related services: filter services by same category, exclude current, slice(0, 4)

    Layout structure:
    ```
    <FloatingCtaButton />  (mobile only)
    <ServiceHero service={service} serviceGroups={serviceGroups} benefits={...} />

    <div className="mx-auto max-w-7xl px-6 py-12 lg:grid lg:grid-cols-3 lg:gap-12 lg:px-8">
      <div className="lg:col-span-2 space-y-12 pb-16">  (main content, pb-16 for mobile floating CTA)
        <ServiceOverview paragraphs={content.overview} />
        <ServiceSigns heading={content.signsHeading} signs={content.signs} />
        <ServiceApproach heading={content.approachHeading} content={content.approachContent} />
        {isCommercialFirst ? (
          <>
            <ServiceAudience variant="commercial" ... />
            <ServiceAudience variant="residential" ... />
          </>
        ) : (
          <>
            <ServiceAudience variant="residential" ... />
            <ServiceAudience variant="commercial" ... />
          </>
        )}
        <ServiceProcess steps={content.processSteps} />
        <ServiceFaq faqs={content.faqs} />
      </div>
      <StickyFormSidebar>
        <LeadForm variant="standard" defaultService={service.slug} serviceGroups={serviceGroups} />
      </StickyFormSidebar>
    </div>

    <RelatedServices services={relatedServices} />
    <ServiceCtaBanner serviceGroups={serviceGroups} defaultService={service.slug} />
    ```

    For the fallback/placeholder content (when getServiceContent is not yet available), create a function that generates minimal placeholder ServiceContent from the Service object so pages render without crashing. Use the service name and shortDescription to fill in content fields.

    IMPORTANT: Do NOT modify src/app/[slug]/page.tsx -- the dispatcher already passes the service prop to ServiceTemplate. The template's interface stays the same (receives { service: Service }).

    Create the stub src/data/service-content/index.ts file that exports getServiceContent function. For now it can just throw an error (the template's try/catch handles this). This stub will be replaced in Plan 04 when all content files are aggregated.
  </action>
  <verify>
    Run `npx next build 2>&1 | tail -20` -- build succeeds with all 63 service pages.
    Visit any service page in dev (`npx next dev` then open localhost:3000/roof-repair) -- verify split hero with form renders, breadcrumbs show, content sections display (placeholder), sticky sidebar visible on desktop, floating CTA visible on mobile viewport.
  </verify>
  <done>ServiceTemplate renders all 63 service pages with split hero + LeadForm, breadcrumbs, 10 content sections (placeholder content), sticky sidebar, floating CTA, related services, and bottom CTA banner. Build passes with zero errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx next build` completes successfully with all static pages
- Service pages render with: breadcrumbs, split hero + form, content sections, sticky sidebar, floating CTA, related services, bottom CTA
- FloatingCtaButton only shows on mobile viewport widths
- Sticky sidebar only shows on lg+ viewport widths
- Commercial-first services (5 IDs) render commercial section before residential
</verification>

<success_criteria>
1. All 63 service pages render without errors via the existing [slug] dispatcher
2. ServiceTemplate displays the 10-section layout defined in CONTEXT.md
3. LeadForm appears in hero (above fold) and sticky sidebar (desktop) and bottom CTA
4. Breadcrumbs show Home > Services > [Service Name]
5. Section components accept typed props matching ServiceContentSchema
6. Placeholder content renders cleanly until content plans populate real data
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-pages/03-01-SUMMARY.md`
</output>
