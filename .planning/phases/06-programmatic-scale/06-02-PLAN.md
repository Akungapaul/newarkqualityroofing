---
phase: 06-programmatic-scale
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/CityServicesGrid.tsx
  - src/components/templates/CityTemplate.tsx
  - src/app/[slug]/page.tsx
  - scripts/audit-orphan-pages.ts
autonomous: true

must_haves:
  truths:
    - "CityServicesGrid links point to combo page URLs (not service page URLs)"
    - "Combo page metadata uses hand-written metaDescription from content when available"
    - "Orphan audit script identifies pages with fewer than 3 inbound internal links"
  artifacts:
    - path: "src/components/sections/CityServicesGrid.tsx"
      provides: "Updated grid linking to combo pages via generateComboSlug"
      contains: "generateComboSlug"
    - path: "scripts/audit-orphan-pages.ts"
      provides: "Orphan page detection script"
      min_lines: 50
  key_links:
    - from: "src/components/sections/CityServicesGrid.tsx"
      to: "src/lib/slug-utils.ts"
      via: "generateComboSlug for combo URLs"
      pattern: "generateComboSlug"
    - from: "src/app/[slug]/page.tsx"
      to: "src/data/combo-content/index.ts"
      via: "getComboContent for hand-written metaDescription"
      pattern: "getComboContent"
---

<objective>
Update navigation links to point to combo pages, switch combo metadata to hand-written descriptions, and create the orphan page audit script.

Purpose: Complete the linking infrastructure so city pages link to combo URLs (not service URLs), combo pages use hand-crafted SEO descriptions, and an audit tool detects orphan pages. This resolves the Phase 4 TODO and establishes the verification tooling needed before content scale-up.

Output: Updated CityServicesGrid with combo URLs, updated metadata generation, orphan audit script.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-programmatic-scale/06-RESEARCH.md
@src/components/sections/CityServicesGrid.tsx
@src/components/templates/CityTemplate.tsx
@src/app/[slug]/page.tsx
@src/data/combos.ts
@src/data/combo-content/index.ts
@src/data/slug-registry.ts
@src/lib/slug-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CityServicesGrid combo URL update + metadata switch</name>
  <files>
    src/components/sections/CityServicesGrid.tsx
    src/components/templates/CityTemplate.tsx
    src/app/[slug]/page.tsx
  </files>
  <action>
    **CityServicesGrid update (resolves Phase 4 TODO):**
    - Add `citySlug: string` prop to CityServicesGridProps interface
    - Import `generateComboSlug` from `@/lib/slug-utils`
    - Change link href from `/${service.slug}` to `/${generateComboSlug(service.slug, citySlug)}`
    - Remove the TODO comment
    - Update CityTemplate.tsx to pass `citySlug={city.slug}` prop to CityServicesGrid

    **Combo metadata switch:**
    In `src/app/[slug]/page.tsx` generateMetadata function, update the 'combo' case:
    - Import `getComboContent` from `@/data/combo-content`
    - Try to get hand-written content: `const content = getComboContent(combo.serviceId, combo.cityId)` in a try/catch
    - If content exists and has metaDescription, use `content.metaDescription` instead of `combo.metaDescription`
    - Fall back to auto-generated `combo.metaDescription` if no content exists
    - Keep using `combo.metaTitle` (no hand-written title exists in content schema)

    This delivers hand-crafted SEO descriptions for the 90 combo pages that have content, while keeping auto-generated descriptions for pages awaiting content.
  </action>
  <verify>
    Run `npx tsc --noEmit` -- compiles cleanly.
    Run `npm run build` -- all pages build successfully.
    Verify CityServicesGrid links generate combo slugs (e.g., on Newark city page, "Roof Repair" links to /roof-repair-newark-nj not /roof-repair).
  </verify>
  <done>CityServicesGrid links to combo pages. Combo metadata uses hand-written descriptions when available. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Orphan page audit script</name>
  <files>scripts/audit-orphan-pages.ts</files>
  <action>
    Create `scripts/audit-orphan-pages.ts` that programmatically computes inbound link counts for every page on the site.

    The script does NOT crawl rendered HTML. Instead, it computes links from the data layer:

    **Link sources to compute:**
    1. **Header/Footer** (every page links to these): homepage, services hub, locations hub, about, contact + footer city links + footer service category links. Count each as 1 inbound link per target.
    2. **Service pages**: Each service page links to related services (same category, max 4). Source: services array filtered by category.
    3. **City pages**: CityServicesGrid links to all 65 combo pages for that city. CityNearbyCities links to adjacent city pages. Source: cities.ts adjacentCityIds.
    4. **Combo pages** (for pages with content): Each combo page links to nearby city combos (via adjacentCityIds), parent service page, parent city page. Source: link-engine.ts functions (import and call them, or replicate the logic).
    5. **Homepage**: Links to representative services (one per category) and all 21 city cards.

    **Output:**
    - Total pages audited
    - Pages with < 3 inbound links (ORPHANS) listed with their current count and sources
    - Summary statistics (min/max/avg inbound links)

    **Run with:** `npx tsx scripts/audit-orphan-pages.ts`

    The script should import from the project's data layer:
    ```typescript
    import { getAllSlugs } from '../src/data/slug-registry';
    import { services } from '../src/data/services';
    import { cities } from '../src/data/cities';
    import { generateComboSlug, generateCityPageSlug } from '../src/lib/slug-utils';
    ```

    Use tsconfig path aliases or relative imports as needed. Add a tsconfig.scripts.json if the main tsconfig doesn't support running scripts directly.

    IMPORTANT: The audit should account for combo pages that DON'T have content yet (they still exist as routes but may not generate outbound links until content is written). Flag these separately as "content-pending orphan risks."
  </action>
  <verify>
    Run `npx tsx scripts/audit-orphan-pages.ts` -- script executes and outputs audit results.
    Verify it reports comparison pages and article placeholder pages as orphans (expected -- they have no content/linking yet).
    Verify combo pages with content (90 currently) show 3+ inbound links from city page CityServicesGrid + header/footer.
  </verify>
  <done>Orphan audit script runs, reports inbound link counts for all pages, flags pages with < 3 inbound links.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- CityServicesGrid links use combo slugs
- Orphan audit script runs and produces report
- Combo pages with content use hand-written metaDescription
</verification>

<success_criteria>
- CityServicesGrid links point to combo page URLs on all 21 city pages
- Combo metadata uses hand-written metaDescription for pages with content
- Orphan audit script identifies all pages with < 3 inbound links
- Build succeeds with existing page count
</success_criteria>

<output>
After completion, create `.planning/phases/06-programmatic-scale/06-02-SUMMARY.md`
</output>
