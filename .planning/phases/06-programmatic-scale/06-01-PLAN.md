---
phase: 06-programmatic-scale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/templates/ComboTemplate.tsx
  - src/components/sections/ComboHero.tsx
  - src/components/sections/ComboOverview.tsx
  - src/components/sections/ComboChallenges.tsx
  - src/components/sections/ComboProcess.tsx
  - src/components/sections/ComboFaqs.tsx
  - src/components/sections/ComboRelatedLinks.tsx
  - src/components/sections/ComboCtaBanner.tsx
  - src/data/linking/link-engine.ts
autonomous: true

must_haves:
  truths:
    - "Combo pages render with split hero (headline+trust left, form right), content sections, and sticky sidebar"
    - "Combo pages show breadcrumbs: Home > Service > Service in City"
    - "Combo pages display nearby city links in ALL THREE formats: card grid in main content, inline text links within content paragraphs, and sidebar section"
    - "Lead form is above the fold in the hero section with defaultService and defaultCity pre-filled"
    - "ComboOverview and ComboChallenges render inline markdown-style links within paragraph text"
  artifacts:
    - path: "src/components/templates/ComboTemplate.tsx"
      provides: "Full production combo page layout with hero, content, sidebar, CTA"
      min_lines: 60
    - path: "src/components/sections/ComboHero.tsx"
      provides: "Split hero with headline+trust+benefits left, LeadForm right"
      min_lines: 50
    - path: "src/data/linking/link-engine.ts"
      provides: "Pure functions for computing internal links from existing data"
      exports: ["getNearbyCityLinks", "getRelatedServiceLinks", "getParentPageLinks"]
  key_links:
    - from: "src/components/templates/ComboTemplate.tsx"
      to: "src/data/combo-content/index.ts"
      via: "getComboContent(service.id, city.id)"
      pattern: "getComboContent"
    - from: "src/components/templates/ComboTemplate.tsx"
      to: "src/data/linking/link-engine.ts"
      via: "imported link functions"
      pattern: "getNearbyCityLinks|getRelatedServiceLinks|getParentPageLinks"
    - from: "src/data/linking/link-engine.ts"
      to: "src/data/cities.ts"
      via: "adjacentCityIds lookup"
      pattern: "adjacentCityIds"
---

<objective>
Build the full production ComboTemplate component with 7 section components and the internal linking engine.

Purpose: Replace the placeholder ComboTemplate with a production-ready hybrid layout that renders combo content with lead form above the fold, differentiated content sections, and computed internal linking blocks. This is the rendering infrastructure that all 1,365 combo pages will use.

Output: ComboTemplate.tsx (production), 7 section components (ComboHero, ComboOverview, ComboChallenges, ComboProcess, ComboFaqs, ComboRelatedLinks, ComboCtaBanner), and link-engine.ts with pure linking functions.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-programmatic-scale/06-CONTEXT.md
@.planning/phases/06-programmatic-scale/06-RESEARCH.md
@src/components/templates/ServiceTemplate.tsx
@src/components/sections/ServiceHero.tsx
@src/components/sections/ServiceFaq.tsx
@src/components/sections/ServiceProcess.tsx
@src/components/sections/ServiceOverview.tsx
@src/components/sections/StickyFormSidebar.tsx
@src/components/sections/FloatingCtaButton.tsx
@src/components/sections/Breadcrumbs.tsx
@src/components/sections/ServiceCtaBanner.tsx
@src/data/combo-content/schema.ts
@src/data/combo-content/index.ts
@src/data/cities.ts
@src/data/services.ts
@src/data/nav-data.ts
@src/lib/slug-utils.ts
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Link engine and section components</name>
  <files>
    src/data/linking/link-engine.ts
    src/components/sections/ComboHero.tsx
    src/components/sections/ComboOverview.tsx
    src/components/sections/ComboChallenges.tsx
    src/components/sections/ComboProcess.tsx
    src/components/sections/ComboFaqs.tsx
    src/components/sections/ComboRelatedLinks.tsx
    src/components/sections/ComboCtaBanner.tsx
  </files>
  <action>
    Create `src/data/linking/link-engine.ts` with three pure functions computed from existing data:
    - `getNearbyCityLinks(service, city)`: Uses `city.adjacentCityIds` to find adjacent cities, returns array of `{name, slug, label}` using `generateComboSlug()`. Include ALL adjacentCityIds (typically 3-5).
    - `getRelatedServiceLinks(currentService, city)`: Returns other services available in same city, grouped by category. Return top 3-5 per category (not all 65). Each entry: `{name, slug, category}` using `generateComboSlug()`.
    - `getParentPageLinks(service, city)`: Returns links to BOTH parent pages: `{servicePage: {name, slug}, cityPage: {name, slug}}` using service.slug and `generateCityPageSlug()`.

    Create 7 section components following ServiceTemplate patterns but with DISTINCT identity:

    **ComboHero.tsx**: Split hero layout (headline+trust left, LeadForm right) matching ServiceHero pattern. Include:
    - Breadcrumbs: Home > [Service Name] (linked to /{service.slug}) > [Service Name] in [City Name] (current, no link)
    - Service category badge above headline (e.g., "Repair & Maintenance" from service.category)
    - H1: "[Service Name]\n in [City], NJ" with city name in copper accent
    - Trust signals list: "Licensed NJ Contractor", "Full Insurance Coverage", "Free Estimates"
    - Phone CTA using PhoneNumber component
    - LeadForm variant="hero" with defaultService={service.slug} and defaultCity={city.id}
    - Dark forest background with CSS gradient + geometric grid + grain (same pattern as ServiceHero)
    - Props: service: Service, city: City, serviceGroups: NavServiceGroup[]

    **ComboOverview.tsx**: Renders content.overview paragraphs. Use alternating parchment/white backgrounds for visual rhythm. Left copper accent border. Props: paragraphs: string[]

    IMPORTANT -- Inline link rendering: ComboOverview MUST parse and render markdown-style inline links within paragraph text. Content paragraphs will contain links like `[Bloomfield](/roof-repair-bloomfield-nj)` that must render as clickable `<a>` tags. Implement a simple inline link parser that converts `[text](url)` patterns to Next.js `<Link>` components within paragraph text. This is required for the "inline text links within content paragraphs" format per locked decision #9.

    **ComboChallenges.tsx**: Renders content.challenges paragraphs under "Local Challenges" heading. Subtle background shift from overview. Props: paragraphs: string[], cityName: string

    IMPORTANT -- Same inline link rendering as ComboOverview: ComboChallenges must also parse and render markdown-style inline links. Extract the link parsing logic into a shared utility (e.g., `renderParagraphWithLinks(text: string)`) that both ComboOverview and ComboChallenges import.

    **ComboProcess.tsx**: Numbered process steps from content.process. Each step as a card with step number badge. Props: steps: string[], serviceName: string

    **ComboFaqs.tsx**: FAQ accordion following ServiceFaq pattern (details/summary). Props: faqs: {question: string, answer: string}[]

    **ComboRelatedLinks.tsx**: Three link sections:
    1. "Nearby Cities" card grid with links to same service in adjacent cities
    2. "Other Services in [City]" categorized list (top 3-5 per category)
    3. "Parent Pages" links to both service page and city page
    4. Empty hidden slot for article link (pre-wire for Phase 7): `{/* Phase 7: Supporting article link will go here */}`
    Props: nearbyCities, relatedServices (grouped by category), parents, cityName

    **ComboCtaBanner.tsx**: Full-width CTA section with forest-dark background, headline "Get Your Free [Service] Estimate in [City]", LeadForm variant="standard". Props: service, city, serviceGroups

    IMPORTANT design identity: ComboTemplate is a DISTINCT third template type. Do NOT copy ServiceHero/CityHero verbatim. Use same design system tokens but with combo-specific identity:
    - Service category badge in hero (service pages don't have this)
    - City name highlighted in copper within headline
    - Compact trust bar (3 items inline, not benefit list)
    - Content sections alternate background (parchment/white) for visual rhythm
  </action>
  <verify>
    Run `npx tsc --noEmit` -- should compile without errors.
    Check that all 7 section components and link-engine.ts exist and export correctly.
    Verify ComboOverview and ComboChallenges both import and use the shared inline link parser.
  </verify>
  <done>All 7 section components created. Link engine exports 3 pure functions. Inline link rendering works in ComboOverview and ComboChallenges. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: ComboTemplate production replacement</name>
  <files>src/components/templates/ComboTemplate.tsx</files>
  <action>
    Replace the placeholder ComboTemplate.tsx with full production component. Follow ServiceTemplate pattern for layout structure:

    ```
    ComboTemplate receives { service: Service, city: City }

    1. Fetch content: getComboContent(service.id, city.id) -- wrap in try/catch, render placeholder message if no content yet
    2. Fetch nav data: getServiceMenuGroups()
    3. Compute links: getNearbyCityLinks(service, city), getRelatedServiceLinks(service, city), getParentPageLinks(service, city)
    4. Render layout:
       - FloatingCtaButton
       - ComboHero (full-width, split layout)
       - Mid-content CTA (between challenges and process): a simple copper-bordered box with "Ready to discuss [service] in [city]? Call us or request a free estimate" + PhoneNumber
       - 2-column layout (lg:grid lg:grid-cols-3):
         - Left (lg:col-span-2): ComboOverview, ComboChallenges, ComboProcess, ComboFaqs, ComboRelatedLinks
         - Right: StickyFormSidebar with LeadForm variant="standard" + nearby cities quick links sidebar section
       - ComboCtaBanner (full-width)
    ```

    Handle the case where content doesn't exist yet (not all 1,365 combos have content during Phase 6 build-out). When getComboContent throws, render a minimal placeholder with service name and city name so the page still builds.

    The mid-content CTA goes between ComboChallenges and ComboProcess -- the natural break point after establishing the problem, before presenting the solution.

    Sidebar quick links section: Show nearby city names as simple text links below the LeadForm in the sticky sidebar, under a "Nearby" heading. This is the THIRD format for nearby city links (sidebar section), complementing the card grid in ComboRelatedLinks and the inline text links within content paragraphs.
  </action>
  <verify>
    Run `npx tsc --noEmit` -- compiles cleanly.
    Run `npm run build` -- existing 90 combo pages with content should render the new template. Remaining combos render placeholder.
    Visit a combo page with content (e.g., /roof-repair-newark-nj) in dev mode to verify layout renders correctly.
  </verify>
  <done>ComboTemplate renders full production layout for pages with content and graceful placeholder for pages without content. Build succeeds with all existing pages. Nearby city links appear in all 3 formats: card grid, sidebar section, and inline text links (once content includes them).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds (existing 90 content pages render full template, remaining render placeholder)
- Combo pages show: hero with form above fold, breadcrumbs, content sections, nearby city links, related service links, sticky sidebar
- Link engine correctly computes nearby cities from adjacentCityIds
- ComboOverview and ComboChallenges render inline markdown-style links as clickable `<a>` tags
- Nearby city links present in all 3 formats: card grid (ComboRelatedLinks), sidebar section (StickyFormSidebar), inline text links (within content paragraphs)
</verification>

<success_criteria>
- ComboTemplate replaced with production component using 7 section components
- Link engine computes all internal links from existing data (no separate link map files)
- Lead form above the fold with service and city pre-filled
- Breadcrumbs show Home > Service > Service in City
- Nearby city and related service links render on combo pages with content
- Inline link parser renders markdown-style links in overview and challenges paragraphs
- Build succeeds with all ~1,492 existing pages
</success_criteria>

<output>
After completion, create `.planning/phases/06-programmatic-scale/06-01-SUMMARY.md`
</output>
