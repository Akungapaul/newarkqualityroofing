---
phase: 05-content-differentiation-engine
plan: 11
type: execute
wave: 4
depends_on: ["05-09", "05-10"]
files_modified:
  - scripts/validate-similarity.ts
autonomous: true

must_haves:
  truths:
    - "Similarity validation runs against all 90 authored combo content files"
    - "Same-service cross-city pairs score below 70% similarity"
    - "Same-city cross-service pairs score below 50% similarity"
    - "Same-category same-city pairs score below 60% similarity"
    - "Validation report clearly identifies any failing pairs for remediation"
  artifacts:
    - path: "scripts/validate-similarity.ts"
      provides: "Fully functional similarity validation CLI"
      contains: "diceCoefficient"
  key_links:
    - from: "scripts/validate-similarity.ts"
      to: "src/data/combo-content/index.ts"
      via: "content import for validation"
      pattern: "getComboContent|combo-content"
---

<objective>
Run the similarity validation script against all 90 authored combo content files, analyze results, and fix any failing pairs.

Purpose: This is the critical validation step that proves the content differentiation engine works. The 90 combos (65 Newark + 25 sample across 5 other cities) provide enough data to validate both cross-city (same service in different cities) and cross-service (different services in same city) similarity thresholds.
Output: Passing validation report with all pairs below tiered thresholds. Any content files with similarity failures are revised to pass.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-content-differentiation-engine/05-RESEARCH.md
@scripts/validate-similarity.ts
@src/data/combo-content/index.ts
@src/data/services.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run validation and analyze results</name>
  <files>scripts/validate-similarity.ts</files>
  <action>
Run the similarity validation script: `npx tsx scripts/validate-similarity.ts`

If the script needs adjustments to work with the actual data (e.g., import paths, cmpstr API differences from what was assumed), fix the script first.

The script should test:
1. **Cross-city pairs (same service):** For each of the 5 validation services (roof-repair, asphalt-shingle-roofing, tpo-roofing-installation, gutter-installation-repair, commercial-roof-installation), compare all city pairs. That's C(6,2) = 15 pairs per service = 75 cross-city comparisons. Threshold: < 0.70.
2. **Cross-service pairs (same city):** For Newark (all 65 services), compare all pairs within the same category. For example, all 10 repair-maintenance services compared pairwise = C(10,2) = 45 pairs. Do this for each of the 8 categories. Threshold: < 0.50.
3. **Cross-category same-city:** For Newark, compare representative services across categories. Threshold: < 0.60.

Capture the full output. Analyze:
- Total pairs tested
- Pass rate per threshold tier
- Worst offenders (highest similarity scores)
- Any failures requiring content revision

If no failures: validation passes, phase success criteria met.
If failures exist: proceed to Task 2.
  </action>
  <verify>Script runs to completion without errors. Output shows pass/fail per pair with similarity scores.</verify>
  <done>Validation report generated showing all similarity scores across all threshold tiers.</done>
</task>

<task type="auto">
  <name>Task 2: Fix similarity failures (if any)</name>
  <files>scripts/validate-similarity.ts</files>
  <action>
If Task 1 identified failing pairs (similarity above threshold):

1. For each failing pair, identify which content sections are most similar:
   - Compare overview arrays separately
   - Compare challenges arrays separately
   - Compare process arrays separately
   - Compare FAQ text separately

2. Rewrite the most similar sections in the file that was authored LATER (preserve the first-authored version as the baseline).

3. Common fixes:
   - If overviews are too similar: restructure the narrative flow, lead with different aspects, use different neighborhood references
   - If challenges are too similar: reorder challenges, lead with a different #1 challenge, use different technical examples
   - If FAQs are too similar: rephrase questions, change the specific details in answers, reference different neighborhoods or building types
   - If process sections are too similar: describe from a different starting point, emphasize different logistics challenges

4. Re-run validation after fixes to confirm all pairs now pass.

If no failures from Task 1: Skip this task (mark as no-op). Log "All similarity checks passed -- no fixes needed."

5. After all fixes (or confirming no fixes needed), run the final validation and capture the complete report output. This report should show:
   - Total combos: 90
   - Total pairs tested: [N]
   - Pass rate: 100%
   - Threshold tiers and their results
   - Worst-case scores per tier (to show margin)
  </action>
  <verify>
Re-run `npx tsx scripts/validate-similarity.ts` and confirm 100% pass rate.
Verify `next build` still passes with any revised content files.
  </verify>
  <done>All similarity checks pass. No pair exceeds its threshold. The content differentiation system is validated. Final report captured.</done>
</task>

</tasks>

<verification>
- Validation script runs against 90 combo content files
- Cross-city same-service similarity: all pairs < 70%
- Cross-service same-city similarity: all pairs < 50%
- Cross-category same-city similarity: all pairs < 60%
- 100% pass rate on final validation run
- `next build` succeeds with all content
</verification>

<success_criteria>
- Similarity validation passes with 100% compliance across all threshold tiers
- The content differentiation engine is proven to produce genuinely unique content
- Phase 5 success criteria #2 is met: "A sample batch of 50 combo pages shows less than 70% similarity between any two pages for the same service"
- Phase 5 success criteria #4 is met: "Content variation patterns are demonstrably different enough to avoid duplicate content detection"
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-differentiation-engine/05-11-SUMMARY.md`
</output>
