---
phase: 04-city-pages-and-local-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/schemas.ts
  - src/lib/types.ts
  - src/lib/hooks/useScrollSpy.ts
  - src/components/templates/CityTemplate.tsx
  - src/components/sections/CityHero.tsx
  - src/components/sections/CityStatsBar.tsx
  - src/components/sections/CityTableOfContents.tsx
  - src/components/sections/CityOverview.tsx
  - src/components/sections/CityResidential.tsx
  - src/components/sections/CityCommercial.tsx
  - src/components/sections/CityNeighborhoods.tsx
  - src/components/sections/CityServicesGrid.tsx
  - src/components/sections/CityTestimonials.tsx
  - src/components/sections/CityProjectSpotlights.tsx
  - src/components/sections/CityFaqs.tsx
  - src/components/sections/CityMapNap.tsx
  - src/components/sections/CityNearbyCities.tsx
  - src/components/sections/CityCtaBanner.tsx
  - src/app/[slug]/page.tsx
autonomous: true

must_haves:
  truths:
    - "CityContentSchema validates city content objects at build time with Zod"
    - "CityTemplate renders all 15 sections in correct order with scroll spy ToC"
    - "City pages show lead form above the fold in hero section"
    - "Residential and commercial sections are visually distinct (forest vs copper theming)"
    - "Google Maps iframe loads lazily with correct city query"
    - "NAP block uses siteConfig as single source of truth"
    - "Nearby cities display as card grid with service links"
    - "Metadata for city pages uses content.metaTitle and content.metaDescription"
  artifacts:
    - path: "src/lib/schemas.ts"
      provides: "CityContentSchema Zod schema"
      contains: "CityContentSchema"
    - path: "src/lib/types.ts"
      provides: "CityContent type export"
      contains: "CityContent"
    - path: "src/components/templates/CityTemplate.tsx"
      provides: "Full city page template with 15 sections"
      min_lines: 80
    - path: "src/lib/hooks/useScrollSpy.ts"
      provides: "IntersectionObserver scroll spy hook"
      exports: ["useScrollSpy"]
    - path: "src/components/sections/CityHero.tsx"
      provides: "Hero with LeadForm above fold"
    - path: "src/components/sections/CityMapNap.tsx"
      provides: "Google Maps iframe + NAP from siteConfig"
  key_links:
    - from: "src/components/templates/CityTemplate.tsx"
      to: "src/data/city-content/index.ts"
      via: "getCityContent(city.id)"
      pattern: "getCityContent"
    - from: "src/components/sections/CityMapNap.tsx"
      to: "src/data/site-config.ts"
      via: "siteConfig import"
      pattern: "siteConfig"
    - from: "src/app/[slug]/page.tsx"
      to: "city content metaTitle/metaDescription"
      via: "generateMetadata city case"
      pattern: "content\\.metaTitle"
---

<objective>
Create the CityContentSchema, all 15 city section components, the CityTemplate, scroll spy hook, and update the slug dispatcher metadata -- everything needed before city content files are populated.

Purpose: Establish the complete city page infrastructure so content plans (04-02 through 04-06) can populate data files and see fully rendered pages immediately.
Output: Working CityTemplate with all section components rendering from CityContent data objects.
</objective>

<execution_context>
@/Users/akungapaul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/akungapaul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-city-pages-and-local-seo/04-RESEARCH.md
@src/lib/schemas.ts
@src/lib/types.ts
@src/components/templates/ServiceTemplate.tsx
@src/components/templates/CityTemplate.tsx
@src/components/sections/Breadcrumbs.tsx
@src/components/sections/FloatingCtaButton.tsx
@src/components/sections/StickyFormSidebar.tsx
@src/components/forms/LeadForm.tsx
@src/data/site-config.ts
@src/data/cities.ts
@src/data/services.ts
@src/data/nav-data.ts
@src/app/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: CityContentSchema, CityContent type, and useScrollSpy hook</name>
  <files>
    src/lib/schemas.ts
    src/lib/types.ts
    src/lib/hooks/useScrollSpy.ts
  </files>
  <action>
    1. Add CityContentSchema to src/lib/schemas.ts (append after ServiceContentSchema). Fields per research:
       - cityId: z.string()
       - heroHeadline: z.string() (e.g., "Expert Roofing Services in Montclair, NJ")
       - heroSubheadline: z.string()
       - stats: z.object({ projectCount: z.string(), servingSince: z.string(), rating: z.string() })
       - overview: z.array(z.string()).min(3).max(6)
       - residential: z.object({ heading: z.string(), content: z.array(z.string()).min(2).max(5) })
       - commercial: z.object({ heading: z.string(), content: z.array(z.string()).min(2).max(5) })
       - weatherChallenges: z.object({ heading: z.string(), content: z.array(z.string()).min(1).max(3) })
       - neighborhoods: z.array(z.object({ name: z.string(), description: z.string().optional() })).min(3).max(15)
       - projectSpotlights: z.array(z.object({ title: z.string(), type: z.enum(['residential', 'commercial']), description: z.string(), details: z.array(z.string()).min(2).max(4) })).min(2).max(5)
       - testimonials: z.array(z.object({ name: z.string(), rating: z.number().min(4).max(5), text: z.string(), service: z.string() })).min(2).max(5)
       - faqs: z.array(z.object({ question: z.string(), answer: z.string() })).min(5).max(8)
       - whyChoose: z.object({ heading: z.string(), reasons: z.array(z.object({ title: z.string(), description: z.string() })).min(3).max(6) })
       - metaTitle: z.string().max(70)
       - metaDescription: z.string().max(160)

    2. In src/lib/types.ts:
       - Import CityContentSchema from schemas
       - Export CityContent type: `export type CityContent = z.infer<typeof CityContentSchema>;`
       - Add CityContentSchema to the re-exports block

    3. Create src/lib/hooks/useScrollSpy.ts:
       - 'use client' directive
       - Export function useScrollSpy(sectionIds: string[], offset = 100) that returns activeId: string
       - Use IntersectionObserver with rootMargin `-${offset}px 0px -80% 0px`
       - useEffect sets up observer on mount, disconnects on cleanup
       - Returns the id of the currently intersecting section
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit` passes with no errors on schemas.ts, types.ts, and useScrollSpy.ts</verify>
  <done>CityContentSchema exists in schemas.ts, CityContent type is exported from types.ts, useScrollSpy hook is ready for use</done>
</task>

<task type="auto">
  <name>Task 2: All 15 city section components</name>
  <files>
    src/components/sections/CityHero.tsx
    src/components/sections/CityStatsBar.tsx
    src/components/sections/CityTableOfContents.tsx
    src/components/sections/CityOverview.tsx
    src/components/sections/CityResidential.tsx
    src/components/sections/CityCommercial.tsx
    src/components/sections/CityNeighborhoods.tsx
    src/components/sections/CityServicesGrid.tsx
    src/components/sections/CityTestimonials.tsx
    src/components/sections/CityProjectSpotlights.tsx
    src/components/sections/CityFaqs.tsx
    src/components/sections/CityMapNap.tsx
    src/components/sections/CityNearbyCities.tsx
    src/components/sections/CityCtaBanner.tsx
  </files>
  <action>
    Create all 14 city section components. Follow the Editorial Trust design system (forest green #1A3A2A, copper #C17F4E, parchment #F5F0E8). Use Tailwind v4 @theme tokens (bg-forest, text-copper, bg-parchment, etc.). Use Cormorant heading font (font-heading) and Cormorant Garamond body (font-body).

    **CityHero.tsx** (server component):
    - Props: city: City, content: CityContent, serviceGroups: NavServiceGroup[]
    - Full-width hero with bg-forest, gradient overlay
    - Breadcrumbs: Home > Locations > {city.name} (use existing Breadcrumbs component)
    - H1: content.heroHeadline (font-heading, text-4xl sm:text-5xl lg:text-6xl, text-parchment)
    - Subheadline: content.heroSubheadline (text-parchment/80)
    - LeadForm with variant="hero", defaultCity={city.id}, serviceGroups prop
    - Zip codes display below form

    **CityStatsBar.tsx** (server component):
    - Props: stats: CityContent['stats'], cityName: string
    - Horizontal bar with 3 stats: projectCount projects, servingSince, rating stars
    - bg-parchment-dark or bg-forest/5 with copper accent borders
    - Responsive: flex row on lg, stack on mobile

    **CityTableOfContents.tsx** (client component -- 'use client'):
    - Props: sections: { id: string; label: string }[]
    - Uses useScrollSpy hook to highlight active section
    - Sticky positioning (top-24 for header offset)
    - Links with smooth scroll behavior (href="#{id}")
    - Active section highlighted with copper left border + copper text
    - Hidden on mobile (shown in sidebar on lg+)

    **CityOverview.tsx** (server component):
    - Props: paragraphs: string[], cityName: string
    - H2: "Roofing in {cityName}: Local Expertise You Can Trust"
    - Renders overview paragraphs as <p> elements with font-body, proper spacing
    - Weather challenges content woven after overview paragraphs
    - Also accepts weatherChallenges prop: { heading: string; content: string[] }
    - Weather section renders with H3 heading and paragraphs

    **CityResidential.tsx** (server component):
    - Props: heading: string, content: string[]
    - H2 with content.residential.heading
    - Visual distinction: bg-forest/5 border-l-4 border-forest rounded-lg p-6 lg:p-8
    - Paragraphs rendered with proper spacing

    **CityCommercial.tsx** (server component):
    - Props: heading: string, content: string[]
    - H2 with content.commercial.heading
    - Visual distinction: bg-copper/5 border-l-4 border-copper rounded-lg p-6 lg:p-8
    - Paragraphs rendered with proper spacing

    **CityNeighborhoods.tsx** (server component):
    - Props: neighborhoods: CityContent['neighborhoods'], cityName: string
    - H2: "Neighborhoods We Serve in {cityName}"
    - Grid layout: 2 cols on sm, 3 on lg
    - Each neighborhood: card with name (H3) and optional description
    - Cards with hover state, border-border, shadow-sm

    **CityServicesGrid.tsx** (server component):
    - Props: cityName: string (services and categories derived from imports)
    - Import services from @/data/services, group by category
    - H2: "Roofing Services Available in {cityName}"
    - Category cards with H3 category name, list of services as links
    - Link to service page slug (NOT combo URL -- combo pages not built yet, per research Pattern 5)
    - Add a comment: // TODO Phase 6: Update links to combo page URLs

    **CityTestimonials.tsx** (server component):
    - Props: testimonials: CityContent['testimonials'], cityName: string
    - H2: "What {cityName} Homeowners Say"
    - Styled visual cards per CONTEXT decision (not carousel)
    - Each card: star rating (filled stars using CSS/Unicode), quote text, name, service type
    - Grid: 1 col on mobile, 2 on md

    **CityProjectSpotlights.tsx** (server component):
    - Props: spotlights: CityContent['projectSpotlights'], cityName: string
    - H2: "Featured Projects in {cityName}"
    - Featured case study blocks per CONTEXT decision
    - Each spotlight: title (H3), type badge (residential/commercial with appropriate color), description, bullet list of details
    - Alternate visual treatment: residential spotlights get forest accent, commercial get copper accent

    **CityFaqs.tsx** (server component):
    - Props: faqs: CityContent['faqs'], cityName: string
    - H2: "Common Roofing Questions in {cityName}"
    - Use <details>/<summary> pattern (same as ServiceFaq, standalone, not FaqAccordion)
    - H3 for each question inside summary
    - Proper spacing and border treatment

    **CityMapNap.tsx** (server component):
    - Props: cityName: string, state: string (default "NJ")
    - Import siteConfig from @/data/site-config
    - Two-column layout on lg (map left, NAP right), stacked on mobile
    - Google Maps iframe: src="https://www.google.com/maps?q={encodeURIComponent(cityName+', '+state)}&output=embed"
    - loading="lazy", aspect-video, rounded-lg, title="Map of {cityName}, {state}"
    - NAP block: siteConfig.companyName, siteConfig.address (street, city, state, zip), siteConfig.phone (tel: link + display)
    - Use <address> tag with not-italic class
    - Business hours from siteConfig.businessHours

    **CityNearbyCities.tsx** (server component):
    - Props: adjacentCities: City[], currentCityName: string
    - H2: "Roofing Services Near {currentCityName}"
    - Card grid: 1 col mobile, 2 on sm, 3 on lg
    - Each card: Link to city page (/{generateCityPageSlug(city.slug)}), city name as H3
    - Show 2-3 popular services per city card (use first 3 services from services array -- these are generic across all cities)
    - Import generateCityPageSlug from @/lib/slug-utils
    - Cards with hover state (border-copper/40, shadow-md)

    **CityCtaBanner.tsx** (server component):
    - Props: cityName: string, serviceGroups: NavServiceGroup[]
    - Full-width bg-forest banner
    - H2: "Ready for Expert Roofing in {cityName}?"
    - Text: "Get your free estimate today..."
    - LeadForm with variant="standard", serviceGroups prop, defaultCity
    - Or simpler: phone CTA + link to contact page (Claude's discretion per CONTEXT)
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit` passes with all new components</verify>
  <done>All 14 city section components exist, compile, and follow the Editorial Trust design system with proper font/color/spacing tokens</done>
</task>

<task type="auto">
  <name>Task 3: CityTemplate assembly and metadata wiring</name>
  <files>
    src/components/templates/CityTemplate.tsx
    src/data/city-content/index.ts
    src/app/[slug]/page.tsx
  </files>
  <action>
    1. Create src/data/city-content/index.ts as a placeholder aggregator:
       - Import CityContentSchema from @/lib/schemas
       - Import CityContent type from @/lib/types
       - Export getCityContent(cityId: string): CityContent function
       - For now, the aggregator imports from content files that DON'T EXIST YET. Use a temporary empty array so the build doesn't crash:
         ```
         const allContent: CityContent[] = [];
         // Content files will be added in plans 04-02 through 04-06
         const contentMap = new Map<string, CityContent>(allContent.map(c => [c.cityId, c]));
         ```
       - getCityContent throws if not found (same pattern as service-content)

    2. Replace src/components/templates/CityTemplate.tsx entirely:
       - Import all 14 city section components
       - Import getCityContent from @/data/city-content
       - Import cities from @/data/cities, services from @/data/services
       - Import getServiceMenuGroups from @/data/nav-data
       - Import FloatingCtaButton, Breadcrumbs
       - Import generateCityPageSlug from @/lib/slug-utils

       Template layout:
       - FloatingCtaButton at top
       - CityHero (full width, with LeadForm above fold)
       - CityStatsBar (full width)
       - Main content area: max-w-7xl, lg:grid lg:grid-cols-4 lg:gap-10
         - Aside (col-span-1, hidden lg:block): CityTableOfContents (sticky)
         - Main (col-span-3, space-y-16): sections wrapped in <section id="..."> elements
           - id="overview": CityOverview (also passes weatherChallenges)
           - id="residential": CityResidential
           - id="commercial": CityCommercial
           - id="neighborhoods": CityNeighborhoods
           - id="services": CityServicesGrid
           - id="why-choose": WhyChoose section (inline H2 + reasons grid, no separate component needed -- or build into CityOverview if simpler)
           - id="testimonials": CityTestimonials
           - id="projects": CityProjectSpotlights
           - id="faqs": CityFaqs
           - id="location": CityMapNap
           - id="nearby": CityNearbyCities
       - CityCtaBanner (full width, end of page)

       Define tocSections array mapping section ids to labels for the ToC.

       Handle case where getCityContent throws (city has no content yet -- during Phase 4 build-up):
       - Use try/catch. If no content, render a "Content coming soon" placeholder similar to the old CityTemplate but with the correct layout shell.

       Adjacent cities resolution:
       - const adjacentCities = city.adjacentCityIds.map(id => cities.find(c => c.id === id)).filter(Boolean)

    3. Update src/app/[slug]/page.tsx generateMetadata:
       - In the 'city' case, try to import getCityContent
       - If content exists, use content.metaTitle and content.metaDescription
       - If getCityContent throws (no content yet), fall back to the existing generic title/description
       - Use try/catch for graceful fallback during incremental content population
  </action>
  <verify>
    Run `npm run build` (or `npx next build`). The build should succeed with all 21 city pages rendering (with "content coming soon" placeholder since no content files exist yet). Verify no TypeScript errors.
  </verify>
  <done>
    CityTemplate renders the full 15-section layout with ToC scroll spy. City pages without content show graceful fallback. Metadata uses content metaTitle/metaDescription when available. The city-content aggregator is ready to receive content files from plans 04-02 through 04-06.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds -- all 21 city pages build as static pages
3. Visiting any city page (e.g., /roofing-in-newark-nj) shows the template shell with "content coming soon" fallback
4. CityContentSchema is exported from schemas.ts and CityContent type from types.ts
5. useScrollSpy hook exists at src/lib/hooks/useScrollSpy.ts
6. All 14 section components exist in src/components/sections/
</verification>

<success_criteria>
- CityTemplate replaces the placeholder with a full 15-section layout
- LeadForm renders above the fold in CityHero
- All section components follow Editorial Trust design (forest/copper/parchment)
- Residential sections use bg-forest/5 + border-forest; commercial uses bg-copper/5 + border-copper
- Google Maps iframe uses keyless embed with loading="lazy"
- NAP block references siteConfig (never hardcoded)
- Service links point to service pages (not combo URLs) with TODO comment for Phase 6
- Build succeeds with graceful content fallback
</success_criteria>

<output>
After completion, create `.planning/phases/04-city-pages-and-local-seo/04-01-SUMMARY.md`
</output>
